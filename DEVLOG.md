# 開發日誌

> 每次改動都要在最上方追加一則。格式：日期 / 做了什麼 / 為什麼 / 改到哪些檔案 / 下一步。
> 專案總覽與架構請看 `CLAUDE.md`。

---

## 2026-08-04 — #017 傀儡師終局處決：鎖血、全場安全圓路徑、一擊必殺

### 使用者需求（原話）

> 傀儡在最後一個階段半血時會鎖血
> boss 跳到戰場中心，並把玩家直線推到戰場邊緣
> 然後boss對全圖施放攻擊，只保留部分區域(小型圓)當作安全區，安全區之間必須是玩家斬擊到的距離，最後一定會可以打到boss
> 安全區中會有特殊傀儡提供體力恢復
> 這招技能持續時間無限直到玩家或boss一方死亡
> 斬擊到boss時，boss會有被處決的放大子彈時間視角，
> 處決後boss會直接死亡

規模大、風險最高的地方是「安全圓路徑會不會卡死玩家」，先用 `EnterPlanMode` 寫計畫，
確認用程式生成路徑（每步距離固定在斬擊可達範圍內）讓連通性天生成立，不會有走不過去的情況。

### 判斷：「半血」怎麼算

最終階段從 `phase3Hp`(0.33) 開始，若「半血」算全場總血量的 50%，會在她還沒進最終階段
就先觸發，跟「在最終階段」矛盾。改採**階段內剩一半**：`triggerHpRatio = phase3Hp / 2 ≈ 0.165`
（總血量的 16.5%）。

### 做了什麼

**觸發與鎖血**：`applyDamage()` 新增檢查——最終階段、血量第一次跌破 `triggerHpRatio`
→ 標記 `executeTriggered`，之後所有傷害直接跳過（血量鎖住）；同時排定 `executePending`，
下一幀 `update()` 一開始就會**強制打斷她當下正在做的任何事**，直接進終局序列。

**演出**：新增 `telegraphExecute` 前搖，時間到後她跳到場地正中央，玩家沿「本體（此刻在
中心）→玩家」方向被**直接位移**（不是物理擊退，保證確實推到牆邊）夾在牆邊；接著
`buildExecutePath()` 從玩家位置到本體位置，用固定步距（`stepDist`，略小於斬擊
`maxDist`）逐步前進、加隨機橫向偏移，生出一串安全圓中心點——連通性靠生成方式保證，
不是靠 A* 之類的搜尋。全場網格灑滿近乎無限存活時間的脈衝傀儡（`PulsePuppet`），
排除掉落在任何安全圓內的格子；安全圓（除了玩家起點跟本體所在的最後一點）都放一隻
同樣近乎無限存活的特殊傀儡，撐住這場沒有時間上限的持久戰。

**一擊必殺**：`tryHit()` 在 `executeActive` 狀態下命中本體，不再呼叫 `applyDamage()`，
改成清場、進 `executing` 狀態定格，回傳的 `HitResult` 多一個 `executed: true`
（`boss/types.ts` 加的可選欄位，Guardian 不用管）。`executing` 倒數
`T.puppeteer.execute.deathDelay` 後才真的變成 `dead`，讓 main.ts 既有的
`!boss.alive → victory` 判斷自然接手，不用另開一條勝利流程。

**main.ts 的處決演出**：偵測到 `hit.executed` 後，設定 `executionT` 倒數（用 `rawDt`
不受時間縮放影響）、強制把 `timeScale` 壓到比一般子彈時間更慢的固定值、觸發較長頓幀。
`Camera` 新增 `execZoomMult` 欄位（`apply()` 的 `ctx.scale()` 乘上這個值），
`execZoomMult` 依正弦曲線（`sin(p·π)`）在演出時間內先放大到峰值倍率再收回 1——
一個公式同時做到「漸入、停留在峰值附近、漸出」，不用切好幾段判斷式。

### 改動檔案

```
修改：src/boss/types.ts（HitResult 加可選的 executed 欄位）
      src/boss/puppeteer.ts（applyDamage 鎖血檢查、telegraphExecute/executeActive/
      executing 三個新狀態、buildExecutePath()/buildExecuteField()、tryHit 的 executeActive
      分支、安全圓持續繪製）
      src/core/camera.ts（新增 execZoomMult，apply() 套用）
      src/main.ts（executionT 狀態、hit.executed 的處決演出分支、timeScale 強制覆蓋）
      src/tuning.ts（T.puppeteer 新增 execute 區塊）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 瀏覽器開兩個章節確認能正常進入戰鬥、無 console 錯誤——但這個機制**要把傀儡師打到
  最終階段的極低血量才會觸發**，一般試玩前段完全不會遇到，這次沒有實際觸發過整套流程，
  安全圓路徑連不連通、處決演出節奏、鎖血後的手感都完全沒有實測

### 下一步建議

1. **這次最需要驗證的就是安全路徑本身**：請使用者刻意把傀儡師打到低血量觸發這招，
   確認每一步是不是真的在斬擊範圍內跳得過去、會不會被 `jitter` 隨機偏移弄到卡進牆角
2. 處決演出的鏡頭放大幅度（`cinematicZoomPeak` 1.9）、演出總長（`cinematicTime` 1.0s）
   是否要更誇張或更精簡，需要實際看過才知道
3. 鎖血後如果玩家遲遲摸不到安全路徑終點，會不會覺得「這招放太久」——雖然特殊傀儡供應
   近乎無限的體力，但沒有時間上限也代表沒有失敗的退場機制，純靠手感調整

---

## 2026-08-04 — #016 放射狀路徑的預警是假的：角度要選招當下鎖定，不是出招瞬間才隨機

### 使用者需求（原話）

> 第一階段的放太陽放射狀光束不要在最後一刻鎖定玩家，那根本無法拿走為躲避
> 要在預瞄線最後一刻鎖定位置，所有的BOSS攻擊都是如此
> 要給玩家足夠的反應時間

### 抓到的真正問題

招式a（放射狀路徑）的預警畫面（`radialTelegraph`）跟實際出招用的角度**根本是兩組不同的隨機數**：
預警畫的是「每幀持續旋轉」的裝飾線條（`this.time * 0.4`），跟出招瞬間才重新 `Math.random()`
算出來的真正路徑角度完全無關。玩家在預警階段看到的畫面沒有任何參考價值，
等於整段 `telegraph` 時間都是「假情報」，跟 #013 抓到的「方向沒有提早鎖定」是同一類錯誤，
只是這次藏在跟 `facing` 無關的獨立隨機數裡，上次的修法沒有覆蓋到。

順手檢查了 Puppeteer 另一個類似風險：招式c（傳送穿越）的安全走廊角度，
原本要等 `fieldDelay` 倒數完、危險場地正式啟動的那一刻才計算，
而 `fieldDelay` 這段緩衝原本設計的目的就是「讓玩家先看清楚走廊在哪」——
但走廊資料在這段時間根本還不存在，等於這個緩衝也是空的，跟放射路徑同一種問題。

### 做了什麼

**招式a**：新增 `Puppeteer.radialBaseAngle` 欄位，在 `updateIdle()` 選招當下（`snapFacing()`
旁邊）就擲骰子鎖定，`updateTelegraphRadial()` 出招時直接讀這個鎖定值、不再重新隨機，
`radialTelegraph()` 預警畫的也是同一個值——現在預警看到什麼，出招就是什麼。
順便把預警線改成隨 `telegraph` 進度從中心往外長，而不是固定長度，讀起來更直覺。
`telegraph` 也從 0.55s 拉長到 0.7s，給多一點反應時間。

**招式c**：把原本「傳送 → 等 fieldDelay → 算走廊＋灑場地」一步到位的邏輯拆成兩段：
傳送完立刻呼叫新的 `computeGauntletCorridors()` 鎖定走廊角度，`fieldDelay` 這整段時間
`corridorTelegraph()` 畫的就是真正會生效的走廊；時間到才呼叫 `spawnGauntletField()`
灑場地，不再重算角度。`fieldDelay` 也從 0.4s 拉長到 0.6s。

**其餘招式檢查過沒有同樣問題**：石之守衛與傀儡師其餘招式的方向已經在 #013 鎖定；
三連踏每發的落點在排定時就存好、預警用同一個值；地裂/砸地/招式d/招式e 的預警本來就是
「這一整塊範圍會變危險」的概觀式圈選，不是誤導成具體方向，不用改。

### 改動檔案

```
修改：src/tuning.ts（radial.telegraph 0.55→0.7；gauntlet.fieldDelay 0.4→0.6）
      src/boss/puppeteer.ts（新增 radialBaseAngle 欄位並在選招當下鎖定；
      updateTelegraphRadial/radialTelegraph 改讀鎖定值；updateTelegraphGauntlet 傳送後
      立刻呼叫新的 computeGauntletCorridors()，spawnGauntletField() 不再重算角度）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 沒有實際重新玩過——預警是否真的看得懂、反應時間夠不夠，需要使用者實際玩過才知道

---

## 2026-08-04 — #015 普攻新增不後跳版本、階段1新招、特殊傀儡縮短存活時間

### 使用者需求（原話）

> 普攻新增一種，往前揮不會後跳的
> 第一階段新增一招
> 減少特殊傀儡存在時間大概5秒

### 一、招式b'：前撲扇形，不後跳

把招式b（`updateLunging`）的扇形命中判定抽成共用的 `forwardStrike()`，
新招式b' 呼叫同一個判定、同樣的傷害/範圍數值（`T.puppeteer.press`，跟 `lunge` 分開設定
方便日後各自調整），差別只在打完**直接收招、不後跳、不留特殊傀儡**——她打完留在原地不撤，
換來的是不必重新欺身就能繼續輸出，跟招式b「打了就跑、留一份回體力獎勵」形成互補。
一樣不受「避免連續同招」限制，可以連續使用。

### 二、招式e：小型旋轉環（階段1解鎖）

第一階段原本只是「跟階段0 同一套招式、出招間隔變快」，沒有真正新機制。這次新增招式e：
原地（不用像招式d 那樣跳到場地中心）直接召喚一圈公轉危險物，比招式d 小圈、少隻、短時間
（`T.puppeteer.spin`：半徑140/5隻/3秒，招式d 是210/8隻/5秒），其中一格一樣是特殊傀儡。
完全重用 `Puppets.spawnOrbitRing()`，等於是招式d 的低風險預告版——玩家在正式面對
最終階段的旋轉環之前，先在階段1 用比較和緩的版本練習「抓時機貼近旋轉中的特殊傀儡」。

### 三、特殊傀儡存活時間 8 → 5 秒

`T.puppeteer.special.life` 調降。這個值現在比招式c 的 `gauntlet.duration`(6.5s) 短，
代表走廊上的特殊傀儡有機會在安全逾時之前就先自己消失——這是刻意接受的取捨，
逼玩家別在走廊裡慢慢摸，跟這次「壓縮生存空間、逼玩家靠斬擊快速移動」的整體方向一致。

### 改動檔案

```
修改：src/tuning.ts（special.life 8→5；新增 press/spin 兩個招式區塊）
      src/boss/puppeteer.ts（forwardStrike 共用扇形判定；新增招式b'/招式e 的狀態與
      update/draw 函式；pickAttack 加兩個候選；fanTelegraph 改成吃參數給b/b'共用）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 沒有實際重新玩過——新招式的節奏、特殊傀儡縮短後走廊里還來不來得及用到，
  都需要使用者實際玩過才知道

---

## 2026-08-04 — #014 特殊傀儡加上存活時間

### 使用者需求（原話）

> 特殊傀儡還是幾秒後會消失 不要讓整個場上都是特殊傀儡

沒被斬到也沒炸到的特殊傀儡原本會一直留在場上；招式a/b/c 又會不斷生新的，
玩家如果不理它們，場上只會越堆越多。

### 做了什麼

`SpecialPuppet` 新增 `life` 倒數（`T.puppeteer.special.life = 8` 秒），時間到自動消失
（一小陣塵土+火花特效，跟被斬中/爆炸的視覺都不同，不會誤會成攻擊判定）。
消失前最後 1 秒（`SPECIAL_FADE_TIME`）會淡出，給玩家「它快不見了」的視覺預告。

招式d（旋轉環）裡會公轉的特殊傀儡沿用 `spawnOrbitRing` 既有的 `life` 參數（跟同一圈的
危險物共用同一個壽命），不是套用通用的 8 秒——這樣它一定跟整圈危險物一起消失，
不會脫離公轉隊形提早不見或活得比隊形還久。`life` 通用值刻意設得比 `gauntlet.duration`(6.5s)
和 `center.ringDuration`(5s) 都長，讓招式c/d 自己的收招清場邏輯正常先生效，這個只是保底。

### 改動檔案

```
修改：src/tuning.ts（special 區塊新增 life）
      src/boss/puppets.ts（SpecialPuppet 加存活倒數與淡出、spawnSpecial 簽章加 life 參數）
      src/boss/puppeteer.ts（三個 spawnSpecial 呼叫點都帶入 T.puppeteer.special.life）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 沒有實際重新玩過——8 秒這個數字、淡出的可讀性都需要使用者實際玩過才知道夠不夠

---

## 2026-08-04 — #013 出招方向選招當下鎖定、斬擊路徑打全部目標

### 使用者需求（原話）

> 攻擊在前一秒確認好方向號不要再提示消失後還轉向玩家
> 斬擊的路徑全部打擊目標不要只打一個

### 一、方向在選招當下鎖定，預警期間不再轉向玩家

之前多個招式（石之守衛的橫掃/普攻雙拳/蓄力重擊/狂暴連撞、傀儡師的招式a/b）在整段預警
期間持續 `faceToward(player.pos, ...)`，代表玩家看到預警的當下，方向還沒定案，
一路追蹤到出招瞬間才真正確定——等於預警形同虛設，沒有給玩家足夠的「讀方向再反應」的時間。

兩隻 Boss 都新增 `snapFacing(target)`（瞬間鎖定朝向，不像 `faceToward` 是漸進的），
在 `updateIdle()` 選招當下（`pickAttack()` 選好、`enter(next)` 之前）呼叫一次。
所有現有招式的預警階段都移除各自的 `faceToward` 呼叫，改成完全依賴這個選招當下鎖定的方向。
石之守衛的衝撞/狂暴連撞原本用「前 55% 追蹤、後 45% 鎖定」的部分追蹤，這次也簡化成
跟其他招式一致的「選招當下就整個鎖死」，不再有兩套不同的鎖定邏輯。

石之守衛的普攻連段（兩拳＋蓄力重擊）現在整套共用同一個鎖定方向，不會拳與拳之間再微調。

### 二、斬擊路徑上的目標全部算數

原本 `main.ts` 的命中結算是「特殊傀儡優先，命中就不再檢查本體」（#012 的判斷），
這次依需求改成**同一斬只要幾何上都在軌跡線段內，特殊傀儡（回體力）跟本體（傷害）都算命中**，
不互斥。`Puppets.trySlashHeal()` 也從「命中第一個就回傳」改成掃過全部特殊傀儡，
一斬如果同時掃到好幾隻，全部都會回體力。

### 改動檔案

```
修改：src/boss/guardian.ts（新增 snapFacing；updateIdle 選招時鎖定方向；
      移除 sweep/punch/punching/heavyPunch/charge/rampage 預警階段的 faceToward）
      src/boss/puppeteer.ts（新增 snapFacing；updateIdle 選招時鎖定方向；
      移除 radial/lunge 預警階段的 faceToward）
      src/boss/puppets.ts（trySlashHeal 掃過全部特殊傀儡，不提早回傳）
      src/main.ts（斬擊命中結算：特殊傀儡與本體同一斬都判定，不互斥）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 沒有實際重新玩過——這是純粹的手感/公平性調整，需要使用者實際感受預警的可讀性
  跟連續命中多個目標的爽感是否符合預期

---

## 2026-08-04 — #012 傀儡師招式全面重做

### 使用者需求（原話）

> 傀儡師的設計被反饋與現在的玩法無關，太無聊了，新版如下
> 1.讓玩家需要靠斬擊的無敵幀位移到下一個安全點，用這個辦法慢慢靠近BOSS
> 2.壓縮地面的生存空間，位移的無敵幀會給玩家更多生存機會
> 3.特殊機制：特別的傀儡，需要顏色區別。攻擊後：會回復體力；該傀儡不會攻擊；玩家靠近時延遲爆炸
> 4.招式如下（a 放射狀路徑＋間隙特殊傀儡／b 前撲扇形＋後跳＋留特殊傀儡／
>   c 傳送對面＋全場穿越＋暈眩／d 中心推牆＋旋轉環）
> 我想加個基本招式給玩家知道特殊傀儡可以斬擊回復體力

規模大（動到整個 Boss 的招式系統），先用 `EnterPlanMode` 寫計畫確認範圍。
這次判斷已知資訊足夠（`puppeteer.ts`/`puppets.ts` 都是這個對話 session 剛寫的），
跳過 Explore 子agent 直接寫計畫，避免重新探索已經在手上的上下文浪費 token。

### 一、全面替換，不是疊加

舊的 6 個招式（環繞召喚/延遲扇形/直線衝刺/拉扯反擊/全圖亂射/普攻）全部移除，
`puppets.ts` 的 `RingPuppet`/`FanPuppet`/`LinePuppet` 也一併移除（新招式都用不到）。
**刻意不保留「連續命中拉扯反擊」**：舊機制是「打太猛就被推開」，跟新設計「玩家要拚命
靠近本體」的核心張力直接衝突，這是這次重做時的判斷，不是遺漏。

### 二、特殊傀儡（`SpecialPuppet`）

貫穿全部招式的核心新機制：青綠色 `#7fe0c9`（跟傀儡師的深紫、一般傀儡的骨白都不同），
不攻擊，玩家進入 `triggerRadius` 會累積引信（離開會衰退，比累積快但不是瞬間歸零），
滿了就爆炸；被斬擊軌跡命中則消滅並回體力（`player.restoreStamina()`，新增的公開方法）。

`main.ts` 的命中結算改成**先檢查特殊傀儡，命中就不再檢查本體**——一斬只認一個目標，
逼玩家瞄準精準二選一，而不是兩者都吃到。

招式b（前撲＋後跳）被指定當作使用者要的「基本招式」：前搖短、單次判定、低風險，
後跳完成後在原地留一隻特殊傀儡，自然教會玩家這個機制，不用另外做第 5 招。

### 三、四個新招式

- **a 放射狀路徑**：新實體 `RayHazard`，從本體向外等角度打出路徑（telegraph 後瞬間拉長、
  持續一段時間才消失），每兩條路徑的中間空隙放一隻特殊傀儡
- **b 前撲＋後跳**：沿用先前「普攻」jab 的骨架，簡化成單次判定（原本是二連擊）
- **c 傳送穿越＋暈眩**：傳送到「玩家位置以競技場中心鏡射」的位置，新實體 `PulsePuppet`
  （定點反覆脈衝）鋪滿全場，排除 `corridorCount` 條安全走廊（保證一條是本體→玩家直線）；
  走廊上擺特殊傀儡；這招期間打中本體會暈眩她（新狀態 `stunned`）並清空危險物；
  設安全逾時避免卡關
- **d 中心推牆＋旋轉環**：跳到場地中心，先一發擊退（沿用 `player.takeDamage` 既有機制，
  沒有另外做新的擊退系統），接著新實體 `OrbitHazard` 召喚一圈公轉危險物，
  其中一格換成會公轉的特殊傀儡（`SpecialPuppet` 支援可選的 `orbit` 參數）——
  這招沿用先前「進最終階段強制先放一次大招」的機制（原本觸發舊的全圖亂射，改成觸發這招）

### 改動檔案

```
重寫：src/boss/puppets.ts（SpecialPuppet/RayHazard/PulsePuppet/OrbitHazard 取代舊三種）
重寫：src/boss/puppeteer.ts（狀態機換血，保留階段/破綻/待機拉扯逼近骨架）
修改：src/tuning.ts（T.puppeteer 的 ring/fan/line/pull/finale/jab 換成
      special/radial/lunge/gauntlet/center）
      src/player.ts（新增 restoreStamina）
      src/main.ts（命中判定先檢查特殊傀儡）
      src/ui/screens.ts（第二章節機制警語文案更新，拿掉拉扯反擊的舊說法）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 瀏覽器開第二章節確認：能正常進入戰鬥、HUD 正常、無 console 錯誤、
  場外提示箭頭在 Boss 離開視野時正確顯示在血條下方（沿用 #011 修好的疊圖邏輯）
- **四個新招式、特殊傀儡的引信節奏、招式c 的走廊寬度與密度、招式d 的公轉速度，
  全部沒有實際玩過**——這些都是牽動手感的即時互動細節，只能截圖驗證不出錯，
  驗證不了好不好玩，需要使用者實際玩一場

### 下一步建議

1. 請使用者整場實際玩過，特別注意：招式c 的安全走廊寬度 (`corridorHalfAngle`) 跟
   場地密度 (`gridSpacing`) 搭配起來走不走得過去；特殊傀儡的引信時間 (`fuseTime`)
   跟斬擊瞄準的節奏搭不搭；招式d 追公轉特殊傀儡的節奏會不會太趕或太鬆
2. 石之守衛的普攻與傀儡師新招式之間的手感落差（一個是圓形基礎攻擊、一個是全套新玩法）
   玩起來會不會覺得兩隻 Boss 風格不統一，需要實際玩過兩章才知道

---

## 2026-08-03 — #011 移除翻滾、鏡頭縮放＋場外提示、傀儡師開場強制大招

### 使用者需求（原話）

> 1.移除所有 閃避的功能 續力斬擊就是閃避的效果
> 2.把攝影機視角放大到玩家無法看到全地圖僅能看到部分，BOSS從不可視的地方攻擊過來時需要有提示
> 3.傀儡師進到最後一個階段的時候先會放一次全圖大招

規模較大（動到核心操作、渲染管線），先用 `EnterPlanMode` + Explore 子agent 全面盤點翻滾相關程式碼
與攝影機系統，並跟使用者確認兩個關鍵決策：右鍵/空白鍵移除翻滾後**完全不綁定**；
斬擊變成唯一閃避手段後**順便調寬無敵窗口與最低耗力**。

### 一、移除翻滾

全面刪除，不留相容代碼：`player.ts` 拿掉 `'dodge'` 狀態、`doDodge`/`updateDodge`；
`core/input.ts` 拿掉輸入綁定，**連帶清掉變成死代碼的 `chargeLocked`/`cancelCharge()`/
`consumeChargeCancel()`**——這套「翻滾打斷蓄力」機制唯一的呼叫來源就是翻滾。
保留空白鍵 `preventDefault`（防捲頁）與右鍵 `contextmenu` 阻擋（防跳出瀏覽器選單），
這兩點是刻意的衛生習慣，不是漏刪。`main.ts` 的 `startRun()` 原本用 `cancelCharge()`
清掉標題畫面殘留的蓄力狀態，改成新增的輕量 `input.resetCharge()`。

斬擊順便調整（`tuning.ts`）：`slash.iFrameEnd` 0.86→0.92、`minCost` 13→8、
`bulletTimeBlend` 0.09→0.06，讓短距離的「防禦性一斬」更堪用。體力經濟的舊註解
（滿力斬64+翻滾25）改寫，**尚未依此重新驗證平衡**。

### 二、鏡頭縮放 + 場外提示

`core/camera.ts` 原本完全沒有縮放（`apply()` 只有 `translate`）。新增 `zoom` 欄位，
在 `resize()` 依 `T.camera.visibleFraction`(0.575) 算出（兩軸各自算一次、取較大值，
確保任一軸都不會顯示超過目標比例），`apply()`/`screenToWorld()`/`worldToScreen()`
都加入縮放換算，`clampToArena()` 的可視半徑也改用縮放後的值。新增 `isOnScreen()`。

兩隻 Boss 各加 `isThreatening` getter（非 idle/recover/dead 視為威脅中，給提示顏色用）。
`hud.ts` 新增 `drawOffscreenBossIndicator()`：Boss 不在畫面內時於螢幕邊緣畫箭頭。

**實測抓到一個 bug**：一開始把箭頭畫在 `ctx.restore()` 之後、`hud.draw()` 之前，
結果 Boss 在正上方時箭頭被 Boss 血條的不透明底色蓋住（血條背景是 `rgba(0,0,0,0.6)` 實心矩形，
後畫的東西會蓋掉先畫的）。改成放在 `hud.draw()` **之後**畫，並把上方的邊界（`topPad`）
拉大到 110（其餘三側維持 46），讓箭頭固定停在血條/部位條 UI 下方，不會疊上去。

範圍只做 Boss 本體的提示，不含個別傀儡/投石/預警圈子物件。

### 三、傀儡師開場強制全圖大招

`puppeteer.ts` 新增 `finaleIntroDone` 旗標（`reset()` 歸零）。`updateIdle()` 選招前
先判斷：剛跨入最終階段（phase 2）且還沒觸發過 → 不透過 `pickAttack()` 的加權隨機，
直接強制進 `telegraphFinale`。不打斷她當下動作，只保證「跨階段後下一次真正選招」是它。

### 改動檔案

```
修改：src/player.ts（移除 dodge 狀態機）
      src/core/input.ts（移除翻滾輸入與 chargeLocked/cancelCharge 死代碼，新增 resetCharge）
      src/main.ts（移除翻滾呼叫，改用 resetCharge；場外提示呼叫，且刻意放在 hud.draw() 之後）
      src/tuning.ts（刪 dodge 區塊；slash 數值調寬；camera 新增 visibleFraction）
      src/ui/hud.ts（刪翻滾耗力刻度線；新增 drawOffscreenBossIndicator）
      src/ui/screens.ts（操作說明刪翻滾列）
      src/core/camera.ts（新增 zoom、isOnScreen；apply/screenToWorld/worldToScreen/clampToArena 改用縮放）
      src/boss/guardian.ts、src/boss/puppeteer.ts（新增 isThreatening）
      src/boss/puppeteer.ts（新增開場強制大招 finaleIntroDone）
```

### 驗證狀況

- `npx tsc --noEmit`、`npm run build:single` 都通過
- 這次瀏覽器分頁實際有拿到 rAF（跟之前幾次背景分頁被暫停不同），趁機做了一些真人操作等級的驗證：
  操作說明確認已無翻滾列、鏡頭縮放明顯生效（看不到全地圖）、傀儡師的「破綻」綠色脈動圈正常顯示、
  斬擊瞄準預覽/耗力/傷害數字正常、**場外提示的疊圖 bug 是這次實測直接抓到並修掉的**
- 傀儡師開場強制大招、翻滾移除後的整體手感、體力經濟是否需要重新收緊——這些沒有實際打完整場驗證

### 下一步建議

1. 請使用者整場實際玩過，特別注意「用斬擊閃避」的容錯感受，以及體力經濟是否因為翻滾拿掉而變太寬鬆
2. 場外提示箭頭的顏色/大小在實際戰鬥的視覺雜訊中夠不夠醒目，需要玩過才知道
3. 傀儡師開場強制大招觸發的時機（可能在她正在做別的招式時才「排隊」觸發）體感是否夠好

---

## 2026-07-31 — #010 兩隻 Boss 新增「普攻」連段、傀儡師全圖亂射改一波多隻

### 使用者需求（原話）

> 傀儡師的全場施放時，一次招喚2-3個傀儡攻擊
> 傀儡師新增普攻讓往前二連擊(扇形)，之後再往後跳一個小距離往前一個扇形
> 石頭BOSS新增普攻，往前揮兩拳都是圓形，然後續力原地前方重擊型
> 讓兩個BOSS攻擊不要重複相同的技能 普攻可以相接

### 一、傀儡師招式5（全圖亂射）改成一波多隻

原本是每 `waveInterval`(0.5s) 逐隻召喚 1 隻（`totalPuppets` 6 隻，共 6 波）。
改成每一波同時召喚 `waveMin~waveMax`(2~3) 隻，`totalPuppets` 同步從 6 拉高到 12
（配合一波變多隻，波數還是拉得夠長，不會一下就打完），讓這個「她不攻擊、你可以自由輸出」
的窗口畫面更擁擠、更有壓迫感，呼應這次「普攻」新增也是在強化整體威脅感的同一個方向。

### 二、兩隻 Boss 都新增「普攻」——近戰基礎連段攻擊

跟其他長讀招的「特殊招式」不同，普攻是節奏快、前搖短、**可以連續使用**的基礎攻擊：
`pickAttack()` 選招時的「避免連續同招」懲罰刻意排除普攻，這樣兩次普攻可以無縫接在一起
（使用者原話「普攻可以相接」）。兩隻 Boss 的普攻形狀刻意不同，呼應「不要重複相同的技能」：

**石之守衛**（`guardian.ts` 新增 `telegraphPunch → punching → telegraphHeavyPunch`）：
原地兩記快速直拳（圓形判定，在她正前方 `punchReach` 處），
打完自動蓄力進入更長的讀招，補上一記範圍更大、傷害更高的正面重擊（一樣是圓形，原地不移動）。

**傀儡師**（`puppeteer.ts` 新增 `telegraphJab → jabbing → hopBack → telegraphJab2`）：
第一次讓她親自出手近戰（先前所有攻擊都是透過召喚的傀儡）——前撲兩記扇形攻擊，
打完往後跳一小段拉開距離、短暫重新預警後，再補上第三記扇形。
跟石之守衛的普攻做出區別：一個是原地圓形雙拳＋蓄力重擊，一個是主動前撲的扇形連段＋跳開變陣。

兩者都只在**打得到人的距離**才會被選中（沿用 #009 的「打不到就不出招」規則），
不受階段/憤怒等級限制——普攻是隨時可用的基礎招。

### 改動檔案

```
修改：src/tuning.ts（guardian 區塊新增 punch*/heavy* 數值；
      puppeteer.finale 加 waveMin/waveMax、totalPuppets 6→12；puppeteer 新增 jab 區塊）
      src/boss/guardian.ts（新增 telegraphPunch/punching/telegraphHeavyPunch 狀態與對應
      update/draw 函式；pickAttack 加普攻候選＋排除連續同招懲罰）
      src/boss/puppeteer.ts（新增 telegraphJab/jabbing/hopBack/telegraphJab2 狀態與對應
      update/draw 函式、doJab()、fanTelegraph()；updateFinaleActive 改一波多隻；
      pickAttack 加普攻候選＋排除連續同招懲罰）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（90.8 kB）
- 用瀏覽器開了兩個章節確認能正常進入戰鬥、HUD 正常、無 console 錯誤
- **兩隻 Boss 的普攻連段完全沒有實測**——瀏覽器自動化分頁沒有 OS 焦點，
  `requestAnimationFrame` 會被整個暫停，無法用截圖驗證出招節奏、命中判定、
  傀儡師全圖亂射一波多隻的畫面密度是否合理

### 下一步建議

1. 請使用者實際玩一場，確認：
   - 兩隻 Boss 的普攻是否真的會被頻繁選中、連段節奏順不順
   - 傀儡師普攻第一次讓她「親自動手」，會不會跟「她主要靠傀儡打人」的核心印象衝突
   - 全圖亂射一波 2~3 隻、總共 12 隻，密度會不會太誇張導致完全沒地方閃
2. 如果普攻搶了太多出招機會（因為不受「避免連續同招」限制，加上距離條件很容易滿足），
   考慮調低 `punchTelegraph`/`jab.telegraph` 候選權重（目前 2.8/2.6），讓她仍會混入其他特殊招式

---

## 2026-07-31 — #009 兩隻 Boss 都改成「打得到人才出招」

### 使用者需求（原話）

> 移動到打的道人的地方在攻擊就好，兩隻BOSS都是這樣改，所以招式都要根據玩家位置遠近進行合理的施放

解讀為：招式選擇要照實際距離判斷「打不打得到」，打不到就別出，先繼續逼近，等真的到了有效範圍再出招——
不能像過去那樣只是「降低機率」，機率再低也還是可能在打不到人的距離硬出招、白白落空。

### 做了什麼

**石之守衛**（`guardian.ts` 的 `pickAttack`）：
- **砸地／橫掃**是以自己為中心的範圍技，離太遠會直接落空——改成距離超出有效範圍時
  **直接不列入候選**（不是只調低權重），逼她要先走近才會用這兩招
- **衝撞／狂暴連撞**需要助跑距離，太貼近時（`d ≤ 130`）沒有意義，一併排除
- 投石／三連踏／地裂本來就會追蹤或覆蓋玩家目前位置，不受這個限制，維持原本的距離加權

**傀儡師**（`puppeteer.ts` 的 `pickAttack`，簽章加上 `player` 才能算距離）：
- **招式1（環繞召喚）**是以她自己為中心的範圍技（`ring.radius + ring.spawnRadius`），
  離玩家太遠一樣直接落空，改成距離內才列入候選
- **招式3（直線衝刺）**太貼近時（`d ≤ 90`）沒意義，排除
- 招式2（扇形，召喚在玩家當下位置附近）與招式5（全圖亂射）跟她自己的位置無關，不受限制

**兩隻 Boss 共同的關鍵改動**：`pickAttack()` 回傳型別都改成 `... | null`——
如果當下距離沒有任何招式打得到（例如玩家一直拉開距離、候選全被排除），
就回傳 `null`，`updateIdle()` 這時**不會出招**，只會繼續走位靠近，
等下一次判斷（每幀都會重算）碰到打得到的距離才出招。這才是「移動到打得到人的地方再攻擊」
的直接實作，而不是換一個小機率繼續硬打。

石之守衛原本理論上不會遇到候選全空的情況（近距離永遠有砸地/橫掃兜底、遠距離永遠有衝撞兜底），
但還是一併做了同樣的 `null` 防呆，兩隻 Boss 的行為模式保持一致。

### 改動檔案

```
修改：src/boss/guardian.ts（pickAttack 加距離硬性排除、回傳型別改 GuardianState | null、
      updateIdle 只在非 null 時出招）
      src/boss/puppeteer.ts（pickAttack 簽章加 player、加距離硬性排除、
      回傳型別改 PuppeteerState | null、updateIdle 只在非 null 時出招）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（86.1 kB）
- 同樣沒有實測——瀏覽器自動化分頁沒有 OS 焦點，遊戲時間會被整個暫停

### 下一步建議

1. 請使用者實際玩一場，確認兩隻 Boss 不會再有「站在很遠的地方硬揮空」的違和感，
   也確認會不會反過來變成她們一直在「靠近中」很久都不出招（如果玩家一直保持拉開距離）
2. 如果覺得判定距離抓得不準（例如砸地明明看起來打得到卻被排除），
   優先檢查 `guardian.ts` 的 `slamRadius + 70` / `sweepRadius + 45`，
   以及 `puppeteer.ts` 的 `ringReach`（`ring.radius + ring.spawnRadius`）這幾個硬性邊界值

---

## 2026-07-31 — #008 傀儡師：待機從「拉扯」再加強為持續逼近施壓

### 使用者需求（原話）

> BOSS要往玩家身上逼近 給玩家壓力

#007 把「一直後退」改成「太近才退、太遠才追、中間站定不動」的拉扯，
但使用者玩過後覺得還不夠——中間那段「站定不動」的安全帶讓她感覺不夠有威脅感。

### 做了什麼

`tuning.ts`／`puppeteer.ts` 的 `updateIdle()`：拿掉「中間安全帶站定不動」，
改成**只要沒有貼到 `retreatDist`(80，幾乎面對面) 就一直在逼近玩家**：
- 超過 `approachDist`(260) 用全速 `approachSpeed`(190) 追
- 260 以內、80 以外，用 `approachSpeed × creepSpeedMult`(0.4) 的慢速持續貼近，
  而不是呆站——讓玩家隨時都感覺得到她在往自己身上壓，不會有「安全距離可以耍廢」的錯覺

只有真的貼到幾乎面對面（`retreatDist`）才會後退一點，避免完全疊在玩家身上。

### 改動檔案

```
修改：src/tuning.ts（retreatDist 100→80、approachDist 360→260、approachSpeed 170→190、
      新增 creepSpeedMult）
      src/boss/puppeteer.ts（updateIdle 拿掉中間安全帶，改成隨時逼近＋近距離慢速貼近）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（85.9 kB）
- 同樣沒有實測——瀏覽器自動化分頁沒有 OS 焦點，遊戲時間會被整個暫停

### 下一步建議

1. 請使用者玩一場確認壓力感是否到位、會不會太黏（例如子彈時間瞄準時被追上的頻率是否合理）
2. `approachSpeed`(190) 介於玩家一般移動(235)與瞄準時移動(235×0.55≈129) 之間，
   是刻意設計：正常走位甩得開她，但長按瞄準蓄力時會被她追上——這是「蓄力有代價」的一部分，
   若覺得蓄力時被追上太快/太慢，優先調這個數字

---

## 2026-07-31 — #007 傀儡師：拉扯式待機取代無腦後退、新增「破綻」窗口

### 使用者需求（原話）

> 敵人一直後退，BOSS的攻擊欲望要強一點，要會跟玩家拉扯，但需要適時的露出破綻

這是實際玩過 #006 版本後的回饋。原本 `updateIdle` 的待機邏輯是「玩家進入 400px 內就一直後退」，
結果變成玩家怎麼追都追不上，Boss 幾乎不主動打人，體感是在單方面逃跑而不是戰鬥。

### 做了什麼

**一、待機從「無腦後退」改成「拉扯」**（`tuning.ts` 的 `retreatDist`/`approachDist`/`approachSpeed`，
`puppeteer.ts` 的 `updateIdle`）：
- 只有近到 `retreatDist`(100，幾乎貼臉) 才會後退——不再是玩家一靠近就跑
- 遠到 `approachDist`(360) 之外，她會主動逼近——玩家不能只靠拉開距離逃避戰鬥，
  這才是使用者要的「拉扯」：你退她就追，你逼近她大多數時候會留下來對戰
- 兩者之間是「安全帶」：站定不動、專心準備出招，不會又進又退顯得毛躁

**二、出招間隔調短**（`idleTime` 1.0→0.5，`idleTimeMultByPhase` 也跟著調整），
讓她決定下一招的頻率大約快一倍，對應「攻擊慾要強一點」。

**三、新增「破綻」機制**（`tuning.ts` 的 `opening` 區塊，`puppeteer.ts` 的 `finishAttack()`）：
連續出招（招式1/2/3、招式4 拉扯反擊）每達到 `opening.every`(2) 次，
下一次收招不再是短暫的 `recover`，而是進入一段明顯更長（`opening.duration` 1.1 秒）、
完全靜止的破綻狀態，並播一圈淡綠色脈動外環＋閃光，畫面上顯示「破綻」字樣。

這解決了「攻擊慾變強之後，會不會變成完全打不到」的疑慮：她本體本來就隨時可以被打中
（沒有無敵機制），但只靠這點不夠——玩家需要一個「肯定會來、看得懂」的節奏點，
而不是靠運氣硬追。破綻窗口刻意排除在招式5（全圖亂射）的邏輯之外——那邊本來就是
更大的專屬輸出窗口，不需要疊加。

連續命中計數（招式4 拉扯反擊的觸發條件）在破綻期間**沒有**特別排除——
玩家在破綻窗口打太猛一樣會觸發拉扯反擊，維持規則一致，只有招式5 期間才是唯一例外
（這點在 #006 就已經決定，這次沒有變動）。

### 改動檔案

```
修改：src/tuning.ts（kiteSpeed/retreatDist/approachDist/approachSpeed 取代 kiteDistance、
      idleTime 調短、新增 opening 區塊）
      src/boss/puppeteer.ts（updateIdle 拉扯邏輯、新增 opening 狀態與 finishAttack()、
      draw() 破綻外環、drawStateLabel 破綻文字）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（85.8 kB）
- 同樣受限於瀏覽器自動化分頁沒有 OS 焦點、`requestAnimationFrame` 被暫停，
  **這次的拉扯手感與破綻節奏完全沒有實測**，數值（`retreatDist`/`approachDist`/
  `opening.every`/`opening.duration`）都是依回饋直接猜的第一版

### 下一步建議

1. **請使用者親自玩一場**確認：追逐感是否好、破綻窗口出現的頻率/長度是否抓得到、
   會不會反而變成太好打
2. 如果還是覺得她跑太快追不上，優先調 `approachSpeed`(170) 與 `kiteSpeed`(150) 的相對關係
3. 如果破綻太密集導致每戰幾乎沒有壓力，調高 `opening.every`；太稀疏則調低

---

## 2026-07-31 — #006 新增 Boss 2「傀儡師」、Title 章節選擇

### 使用者需求（原話）

> 由傀儡師進行BOSS發想
> 只有打到本體才會受傷
> 平時招喚傀儡在身旁攻擊
> 血量分三個階段,每個階段都會有新的攻擊模式，每個階段都會更具有進攻性
> 1.招喚三個傀儡在身邊，攻擊範圍為自身與傀儡身邊一圈
> 2.招喚一個傀儡在主角身邊,延遲攻擊，範圍一個扇形
> 3.向前一個直線範圍，沿途招喚傀儡斬擊
> 4.被主角打到幾下後會往一個方向，招喚一個傀儡，把自身拉過去沿途進行攻擊
> 5.最後階段會，全圖隨機位置招喚傀儡攻擊一個圓，此時自己不攻擊，玩家可以對她進行攻擊
> 6.傀儡師的顏色要和傀儡不同顏色，樣子也要有點差別(不多)

進場前先用 `EnterPlanMode` 確認了兩個關鍵決策（其餘判斷由我依專案慣例決定，記錄於下）：
- 目前沒有 Boss 選單/存檔系統 → 使用者選擇：**Title 畫面加第二個入口**（兩隻 Boss 並存，為未來正式選單鋪路）
- 招式4（連續命中拉扯反擊）觸發範圍 → 使用者選擇：**全程都會觸發**，門檻可依階段微調

### 一、命中模型：跟石之守衛完全相反

傀儡師沒有部位/核心倍率分層，`bodyMult = 1.0`——斬擊算出多少傷害就是多少。
她的難度來自「打得到」而不是「打得對」：待機時**遠離玩家（kite）**，
逼玩家在召喚的傀儡之間穿梭找輸出機會，而不是像石之守衛一樣站著等核心暴露窗口。

**傀儡不可被打中、沒有碰撞體**（`src/boss/puppets.ts`）——只有牠們的攻擊判定會傷玩家，
斬擊打穿傀儡不會有任何效果。沿用 `boss/projectiles.ts` 既有的
「spawn → telegraph → 一次性傷害（`hitPlayer` 旗標）→ dead 清除」模式，新增三種傀儡：
`RingPuppet`（環形 AoE，用於招式1/5）、`FanPuppet`（延遲扇形，招式2）、
`LinePuppet`（沿途斬擊，招式3/4 共用）。

`HitResult.kind` 回報重用既有的 **`'core'`**，不是新 kind——本體命中在敘事上就是
「正確、高回饋的目標」，直接沿用 main.ts 既有的金色特效與大數字回饋。

### 二、招式與三階段

血量三階段（0/1/2，門檻 66% / 33%）逐階解鎖：
階段0 只有招式1（環繞召喚：3 隻傀儡＋她本人各一圈 AoE）；
階段1 加招式2（延遲扇形）與招式3（直線衝刺＋沿途傀儡）；
階段2（最終階段）加招式5（全圖隨機召喚，她自己完全不出招——整場最大的自由輸出窗口）。

招式4（連續命中拉扯反擊）獨立於階段之外、全程可觸發，**唯一例外是招式5 期間不會被打斷**——
那是刻意留給玩家的輸出窗口，若被反擊打斷等於收回「她不攻擊」的承諾，這個例外在
`Puppeteer.registerHit()` 有註解說明。方向固定選「遠離玩家」，重用招式3 的沿途傀儡機制。

每階段額外調快出招間隔（`idleTimeMultByPhase`）、提高傷害（`dmgMultByPhase`），
呼應「每個階段更具進攻性」。

### 三、外觀

傀儡師：偏瘦長菱形/兜帽輪廓，深紫色 `#5b3a68`。
傀儡：矮胖橢圓，骨白/舊木色 `#d8cfc0`，加十字縫線裂痕暗示木偶關節。
兩者剪影/配色都不同，但共用同一套「純幾何拼接」語言，符合「有差別但不多」。

### 四、雙 Boss 架構

`main.ts`/`ui/hud.ts` 各自定義 `type AnyBoss = Guardian | Puppeteer`——
沒有另外包一層抽象介面，目前只有兩隻 Boss，聯合型別＋`instanceof` 分支比硬做通用介面務實。

`tryHit(a, b, baseDamage, playerPos)` 的簽章兩者現在一致（石之守衛忽略 `playerPos`，
只是為了跟傀儡師共用同一組呼叫簽章）；`HitResult` 型別搬到新的 `boss/types.ts`，
避免兩個 boss 檔案互相 import。

HUD 的 `rage`/`rageRatio` 欄位兩者共用（石之守衛＝憤怒等級，傀儡師＝目前階段，
`phase` getter 依血量比例算 0..2），但部位破壞條這類真正 Guardian 專屬的畫面
用 `instanceof Guardian` 分支處理。

Title 畫面（`ui/screens.ts`）新增兩個可點擊的章節入口（`hitTestChapter`），
滑鼠懸停哪個入口，下方的操作說明機制警語就跟著換；按任意鍵仍固定進第一章，
保留原本鍵盤直接開始的手感。`drawOutcome` 的勝負文案改成外部傳入，
兩章節各自的說法：「守衛崩解」/「傀儡瓦解」。

### 改動檔案

```
新增：src/boss/types.ts, src/boss/puppets.ts, src/boss/puppeteer.ts
修改：src/boss/guardian.ts（HitResult 改 import、tryHit 簽章加 playerPos、新增 name 欄位）
      src/tuning.ts（新增 T.puppeteer 整區塊）
      src/main.ts（AnyBoss、chapter 狀態、startRun(chapter)、章節點擊判斷、勝負文案參數化）
      src/ui/hud.ts（AnyBoss、instanceof 分支畫部位條/憤怒 vs 階段菱形、boss.name）
      src/ui/screens.ts（章節選擇 hitTestChapter、機制警語依章節切換、drawOutcome 文案參數化）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（84.9 kB）
- 用瀏覽器自動化開了兩個章節：石之守衛（回歸測試）與傀儡師都能正常進入戰鬥，
  HUD 名稱/菱形數量/血條都正確，無 console 錯誤
- **傀儡師完全沒有實際試玩過**：自動化瀏覽器分頁沒有 OS 焦點，Chrome 會整個暫停
  `requestAnimationFrame`（自繪準心消失可以確認），無法用截圖驗證任何即時性的行為
  （出招節奏、階段切換、連續命中反擊、最終階段輸出窗口）。這些都需要使用者親自玩一場。

### 下一步建議

1. **先試玩傀儡師整場**——`T.puppeteer` 整區塊的數值全是第一版猜測：
   - `kiteSpeed`(140) / `kiteDistance`(400) 是否讓她「追得到但追得辛苦」，還是根本追不上或太好抓
   - 招式1~5 的傷害/範圍/延遲是否讀得懂、躲得掉
   - `pull.hitStreakThresholdByPhase`([3,3,2]) 觸發是否太頻繁或太不頻繁
   - 階段2 全圖亂射的輸出窗口是否真的划算（跑位到本體要花多久）
2. 兩隻 Boss 的強度目前沒有互相校準過（傀儡師 680 HP、石之守衛 820 HP 都只是猜測）
3. 加音效——兩隻 Boss 都還是完全無聲

---

## 2026-07-28 — #005 空白鍵改為翻滾、新增蓄力取消區

### 使用者需求（原話）

1. 空白鍵是改成右鍵衝刺
2. 新增續力取消區，玩家滑鼠左鍵放開位置於自己身上時，不進行續力攻擊，
   需要給玩家一個小型的判定區標示

### 一、空白鍵改綁翻滾（修正 #004 的誤解）

#004 把空白鍵做成「與左鍵相同 = 蓄力」，這是我讀錯了需求。
使用者這次明確指出：**空白鍵 = 右鍵 = 翻滾**。已改正。

連帶把 `core/input.ts` 的蓄力狀態機簡化：
- 移除 `ChargeSource` 型別與雙鍵互相取消的邏輯（蓄力現在只有左鍵）
- `chargeSource` → `chargeActive`（單純的布林）
- `consumeRightPress()` → **`consumeDodgePress()`**（右鍵或空白鍵都會觸發）
- **保留 `chargeLocked`**：翻滾打斷蓄力後仍需鎖定到左鍵放開，否則會誤斬（見 #004 記錄的 bug）

### 二、蓄力取消區

放開左鍵時，若游標落在角色周圍 `cancelRadius: 48` 內 → **不出刀、不耗體力**。

**實作重點**：
- `SlashPlan` 新增 `cancel: boolean`，取消區判定**優先於其他所有判定**
- `planSlash()` 新增第三個參數 `cursorDist`（游標到角色的實際距離）
  - 不能直接用 `len(aimVec)`：`drag` 瞄準模式下 `aimVec` 是拖曳量，
    長度不代表游標離角色多遠。必須由呼叫端獨立算好傳入
- `cancelRadius(48) < minDist(60)`，避免與「最小距離斬擊」的判定重疊
- `player.plan()` / `player.doSlash()` 簽名同步加上 `cursorDist`

**視覺標示**（使用者要求的「小型判定區標示」）：
- 蓄力時角色身上**一直**顯示一圈低調虛線圈——讓玩家知道「反悔」這個選項隨時存在，
  而不是要自己摸索才發現
- 游標進入時轉為：紅色半透明填充 + 脈動實線 + **大大的 ✕** + 下方「放開以取消」文字
- **進入取消區時不再畫瞄準線**——畫面上不該同時出現「要斬」和「不斬」兩種訊號
- 游標本身也換成紅色 ✕，外環放大並反向旋轉，威力進度弧隱藏
- 體力條旁的文字改為「放開以取消　不消耗體力」
- 取消時播一圈**向內收**的冷色環（和斬出去的外擴刀光相反，方向語言一致）

### 改動檔案

```
修改：src/core/input.ts（空白鍵改綁翻滾、蓄力狀態機簡化）
      src/tuning.ts（cancelRadius）
      src/slash.ts（SlashPlan.cancel、planSlash 新增 cursorDist 參數）
      src/player.ts（plan / doSlash 簽名）
      src/main.ts（cursorDist 計算、取消分支、cancelFx）
      src/ui/hud.ts（取消區繪製、游標取消狀態、體力條文字）
      src/ui/screens.ts（操作說明更新）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（68.5 kB）
- **仍未實際試玩**

### 下一步建議

1. 試玩確認 `cancelRadius: 48` 的手感——太大會誤觸（想近距離斬卻變成取消），
   太小則不好瞄。它的上限是 `minDist`(60)
2. `brokenPartMult: 1.6` 仍待驗證（見 #004）
3. 加音效

---

## 2026-07-28 — #004 蓄力取消、空白鍵、瞄準環游標、防重疊、破壞處變弱點

### 使用者需求（原話）

1. 右鍵續力可以按左鍵取消續力
2. 空白鍵跟左鍵相同功能
3. 滑鼠位置新增一個顯眼的圖示
4. 角色跟怪物不可以重疊會看不到角色
5. 部位破壞後可以造成大量的傷害 —— **「不然沒有優點」**

### 需求 1 的解讀（有歧義，記錄我的判斷依據）

原文寫「**右鍵**續力可以按**左鍵**取消」，但當時的操作是「左鍵蓄力、右鍵翻滾」，
而需求 2 又說「空白鍵跟左鍵相同功能」——這確立了**左鍵是蓄力鍵**。
所以左右應為筆誤，真正的需求是：**蓄力中要能取消，不要被迫斬出去**。

實作時發現這其實是一個**真實的 bug**：
蓄力中按右鍵翻滾後，若玩家在翻滾結束（0.34 秒後）才放開左鍵，
`consumeLeftRelease()` 仍會觸發、此時 `canAct` 已恢復 → **意外斬出一刀並吃掉體力**。
想閃避卻被迫送出攻擊，體感非常糟。

**做成三種取消方式**，涵蓋所有可能的原意：
- 蓄力中按**右鍵** → 取消蓄力並翻滾（一個動作同時達成取消與閃避）
- 蓄力中按**另一個蓄力鍵**（左鍵↔空白鍵）→ 純取消，不出刀、不耗體力、不翻滾
- 遊戲邏輯主動取消：`input.cancelCharge()`

若我的解讀有誤，請直接說，改回來很快。

### 做了什麼

**1 + 2. 蓄力輸入模型重寫（`core/input.ts`）**

不再用「左鍵按住 / 放開」的直接映射，改為 `chargeSource` 狀態機：

- `chargeSource: 'mouse' | 'space' | null` —— 記錄是哪個鍵啟動了這次蓄力
- 取消時把 `chargeSource` 設為 null，並鎖定 `chargeLocked`，
  **直到兩個蓄力鍵都放開才解鎖**
- 因此取消後放開按鍵不會觸發 `consumeChargeRelease()`，不會誤斬；
  也不會「取消完手還按著，結果立刻又開始蓄力」
- 新 API：`chargeDown` / `consumeChargeRelease()` / `consumeChargeCancel()` / `cancelCharge()`
- `leftDownAt` → `chargeStartAt`（drag 瞄準模式改用它，空白鍵蓄力也能正常運作）
- 順手修掉一個問題：在標題畫面按空白鍵開始遊戲後，該鍵仍按著會直接變成蓄力
  → `startRun()` 呼叫 `input.cancelCharge()`

**3. 瞄準環游標（`ui/hud.ts` 的 `drawCursor`）**

從原本的簡單十字改成多層瞄準環：
- 四段**旋轉**弧環（蓄力時旋轉加快、環收縮，像在「上膛」）
- 蓄力時環上長出一圈**威力進度弧**
- 四條內指標，蓄力時往中心收緊
- 中心菱形 + 柔光底
- **每一層都先描一次深色外框再畫主色**——確保在暗地板、亮火花、紅色預警上都看得見
- 顏色隨威力由白 → 金 → 橙紅

**4. 玩家與 Boss 不重疊（`main.ts`）**

兩個機制並存：
- `separatePlayerFromBoss()`：玩家進入 Boss 碰撞圓時被推到邊緣外（處理常態）
- **玩家改畫在 Boss 之上**（處理斬擊穿越時的短暫重疊）
- **斬擊位移期間不推開**——那是穿越攻擊，而且只有 0.13 秒。
  若強制推開，斬擊會被 Boss 擋住停在體外，破壞核心機制

**5. 破壞處變成永久弱點（`brokenPartMult: 1.6`）**

使用者一句「不然沒有優點」點出了 #002 設計的真正缺口：
破壞部位只會激怒 Boss，玩家沒有任何理由去拆——部位系統退化成純懲罰機制。

現在破壞後的部位留下**裸露傷口**，命中造成 **1.6 倍**傷害（僅次於核心的 2.0）：

- `tryHit` 判定優先序改為：**暴露核心(2.0) > 裸露傷口(1.6) > 未破壞裝甲(0.3~0.5) > 本體(0.12)**
- **傷口優先於裝甲**：玩家是刻意瞄準傷口，不該因為旁邊的裝甲剛好更近而被吃掉
- `HitResult.kind` 新增 `'wound'`
- 視覺：傷口的發光斷面加亮，加上**脈動的弱點環 + 中心亮點**，與核心的視覺語言一致
- HUD 部位條破壞後顯示「**XX・弱點**」（不再只是「破壞」）並改用暖色漸層
- 命中傷口：橘紅爆裂 + 雙環 + 閃光，份量介於核心與裝甲之間
- 破壞橫幅副標改為「**弱點暴露 —— 但守衛更加憤怒了**」，一句話講完這個交換
- 順帶讓「繞背打腿」有了價值：腿破壞後，背面就是一個 1.6 倍的輸出點

這條規則同時修正了 #002 遺留的平衡風險（憤怒 4 級時傷害 +80% 可能贏不了）：
拆得越多、Boss 越強，但玩家的穩定輸出點也越多。

### 改動檔案

```
重寫：src/core/input.ts（蓄力狀態機）, ui/hud.ts 的 drawCursor
修改：src/main.ts（新輸入 API、分離邏輯、繪製順序、wound 特效、橫幅副標）
      src/tuning.ts（brokenPartMult）
      src/boss/guardian.ts（tryHit 判定優先序、HitResult.kind）
      src/boss/parts.ts（傷口的弱點視覺）
      src/ui/screens.ts（操作說明與機制警語更新）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（67.5 kB）
- **仍未實際試玩**

### 下一步建議

1. 試玩並驗證 `brokenPartMult: 1.6` 是否恰當——
   太高會讓「拆光四個部位」變成無腦最優解，太低則回到「沒有優點」的老問題
2. 確認新游標在紅色預警區上是否仍清楚（深色描邊應該夠，但要實測）
3. 加音效——仍然是最後一塊拼圖

---

## 2026-07-28 — #003 特效系統、單一檔案打包

### 使用者需求（原話）

1. 「我開啟這個專案是怎麼打開 可以本地打開嗎」
2. 「替 BOSS 的攻擊都坐上特效 + 玩家角色攻擊也都坐上特效」

### 一、本地打開（需求 1）

**問題**：`npm run build` 出來的 `dist/index.html` 雙擊是打不開的，只會看到黑畫面。
**原因**：產物用 `<script type="module" src="./assets/xxx.js">`，
瀏覽器對 `file://` 下的 module script 有 CORS 限制，會直接擋掉外部 JS。

**解法**：新增 `build-single.mjs`，把 JS 內嵌成 inline module 壓成單一 HTML。
inline module 沒有任何外部請求，`file://` 就能正常執行。

- 新增 `vite.config.ts`（`base: './'`，讓產物不綁死在網站根目錄）
- 新增 `build-single.mjs`（同時處理外部 JS 與外部 CSS，並跳脫 JS 內可能出現的 `</script>`）
- `package.json` 加 `build:single` script
- 產物：`TitanSlash.html`（63.8 kB），雙擊即玩，也可以整個檔案傳給別人

### 二、特效系統（需求 2）— 新檔 `src/fx.ts`

建了一個全遊戲共用的特效單例 `fx`，五種基本元件：

| 元件 | 用途 |
|------|------|
| `burst / spark / dust / debris` | 粒子：火花（發光）、塵土（地面層）、碎石（旋轉多邊形 + 重力） |
| `ring` | 擴散環，尾段快前段慢的緩動，有爆發力 |
| `arc` | 扇形刀光，可設定沿扇形「掃過去」而非整片同時亮 |
| `slash` | 玩家斬擊的柳葉形刀光（兩端尖、中段鼓起）＋ 中央高光 |
| `flash` | 徑向漸層閃光 |

**關鍵設計**：
- 分 `ground` / `over` 兩層，塵土畫在實體底下、火花畫在實體之上
- `fx.update()` 吃**已縮放的 dt** → 子彈時間裡特效變慢、命中頓幀時定格。這是刻意的
- 粒子總量上限 900，超過丟棄最舊的，避免長時間戰鬥掉幀
- 發光粒子用 `globalCompositeOperation = 'lighter'`，且做了批次切換避免每顆粒子都 save/restore

**玩家特效**：
- 斬擊：刀光（粗細/亮度隨威力）＋ 起步踏塵 ＋ 前向氣流火花；威力 > 0.62 額外給一圈衝擊環
- 翻滾：蹬地起步塵 ＋ 滾動中持續掃塵 ＋ **無敵幀期間額外的藍色殘光**（用眼睛就能確認自己無敵）＋ 落地收勢塵
- 受擊：紅色衝擊火花 ＋ 短促白環 ＋ 閃光
- 移除了舊的 `Trail` 殘影系統，改由 `fx.slash` 統一負責

**Boss 七招各自的特效**：
1. **衝撞** — 蓄力刮塵（越接近發動越急）→ 起步爆發 → 高速拖尾（憤怒時帶紅火星）→ **撞牆：24 顆碎石 + 揚塵 + 200px 衝擊環 + 強閃光**（整場最重的一擊）
2. **砸地** — 地面環 + 放射碎石 + 揚塵 + 中心閃光，雙層環
3. **橫掃** — 沿扇形掃過去的刀光 ＋ 沿弧 7 個點撒出的塵土 ＋ 扇形火花
4. **投石** — 投擲甩臂塵 + 碎屑 + 小環；飛行拖尾；**爆炸：碎片 + 火花 + 地面環 + 閃光**
5. **三連踏** — 每次踏擊的地面環 + 碎石 + 塵 + 閃光
6. **地裂** — 蓄力時地面碎石被震得往上跳（最明顯的「快跑」訊號）→ 發動時 240px 大環 + 30 碎石 + 全場閃光 → **裂縫波前持續掀起岩屑**
7. **狂暴連撞** — 蓄力時赤紅火星**向內收束**（宣告最兇的一招）→ 每段衝刺起步爆發 → 赤紅拖尾 → 撞牆碎石

**其他**：
- 核心暴露期間持續噴金色火星——「快打這裡」在畫面上不可能被忽略
- 部位破壞：26 碎石 + 34 火花 + 揚塵 + 雙層環 + 強閃光
- 命中依部位分級：**核心 = 金色爆裂 + 雙環 + 閃光**、**部位 = 橘色火花 + 石屑**、**身體 = 冷色稀疏火花**（視覺上直接說「這裡打不動」）
- 火花噴發方向沿**斬擊軌跡方向**，不是隨機全向
- 勝利：48 碎石 + 44 火花 + 320px 大環 + 0.2 秒頓幀

### 改動檔案

```
新增：src/fx.ts, vite.config.ts, build-single.mjs
修改：src/main.ts（fx 整合、命中特效、勝負特效）
      src/player.ts（斬擊/翻滾/受擊特效，移除舊 Trail 系統）
      src/boss/guardian.ts（七招特效 + 核心暴露火星 + 部位破壞爆發）
      src/boss/projectiles.ts（投石拖尾與爆炸、地裂波前岩屑）
      package.json（build:single）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npm run build:single` 通過，產出 `TitanSlash.html`（63.8 kB）
- **仍未實際試玩**。特效數量偏多，需確認：(a) 是否蓋住 Boss 的攻擊預警（那是玩家的生命線）、(b) 大量粒子同時存在時的幀率

### 下一步建議

1. **試玩並確認特效沒有蓋掉預警可讀性**——這是加特效最大的風險。
   若地面預警看不清，優先調降 `fx.dust` 的數量或把塵土 alpha 從 0.62 降低
2. 平衡仍未驗證（見 #002 的三個風險點）
3. 加音效——特效做完後，聲音是最後一塊拼圖，且七招全靠視覺辨識的負擔會明顯降低

---

## 2026-07-28 — #002 部位破壞系統、憤怒機制、招式擴充、難度收緊

### 使用者需求（原話）

1. 斬擊和翻滾都要有位移無敵幀
2. 敵人新增多個部位，斬擊累積破壞值可以破壞部位
3. 敵人招式多新增幾個
4. 場地縮小一點，現在太大打起來有點簡單
5. Boss 的動作加快，讓玩家的體力使用上「吃不飽餓不死」
6. **怪物的攻擊在破壞部位後才會增加，傷害也會變高，要讓 Boss 視覺上有發狂的感覺**

### ⚠ 一個設計方向的翻轉（重要）

第 6 點推翻了 #001 沒有明說、但我在實作部位系統時預設的方向。
我原本做成 **「破壞部位 = 削弱 Boss」**（斷手臂 → 失去砸地，斷腿 → 移動變慢）。
使用者要的是相反的：**「破壞部位 = 激怒 Boss」**。

**已全部翻轉為使用者的版本。** 這個方向確實更好：

- 削弱版本裡，拆部位是無腦收益，玩家沒有抉擇——拆就對了
- 激怒版本裡，拆部位變成真正的風險交換：你用傷害和優勢，換來一隻更快更痛的守衛

`tuning.ts` 的 `brokenLegs`（減速效果）已整段移除，改為 `rage` 區塊。
**不要改回去**，`CLAUDE.md` 第 5 節第 3 點已把這件事寫成硬性約定。

### 做了什麼

**1. 位移無敵幀（需求 1）**
- `T.slash` 的布林 `iFrame` 改成區間制 `iFrameStart / iFrameEnd`（0.0 ～ 0.86），與翻滾同結構
- 斬擊現在是「穿過去」而不是「站著砍」，但**收招瞬間會解除無敵**——保留風險
- 翻滾無敵區間微調為 0.06 ～ 0.78

**2. 可破壞部位系統（需求 2）— 新檔 `boss/parts.ts`**
- 4 個部位：核心護甲(300) / 左臂(155) / 右臂(155) / 腿部(185)
- 斬擊命中部位時，**原始傷害**累積為破壞值；本體 HP 依部位的 `dmgMult` 受傷
- 破壞瞬間：額外傷害（integrity × 0.18）＋ 踉蹌硬直 0.95 秒 ＋ 噴出 34 顆火星
- 破壞核心護甲 → **核心永久暴露**，不必再誘導撞牆
- 命中判定優先序：暴露的核心 > 最近的未破壞部位 > 身體
- 破壞後的斷面持續洩出熾熱能量（脈動的橘紅光暈）

**3. 招式擴充（需求 3）— 新檔 `boss/projectiles.ts`**
從 2 招擴到 7 招，依破壞部位數解鎖：
- 既有：衝撞、砸地
- 新增：**橫掃**（近距扇形）、**投石**（落點預警圈，帶玩家速度前導）、**三連踏**（逐次逼近的三發）、**地裂**（9 道放射波前）、**狂暴連撞**（三段高速衝刺，撞牆不失衡）
- 衝擊波、投石、地裂抽成獨立的 `Projectiles` 管理器，傷害由 Boss 動態注入（才吃得到憤怒加成）

**4. 場地縮小（需求 4）**
- 1400×1000 → **1100×820**，牆厚 24 → 22
- 連帶縮短 `quakeMaxLen`(900→720) 與 `shockwaveMaxRadius`(460→400)

**5. Boss 加速 + 體力收緊（需求 5）**
- 所有預警時間下修：衝撞 0.72→0.56、砸地 0.62→0.5、橫掃 0.46→0.36、投石 0.68→0.52、三連踏 0.44→0.34、地裂 0.85→0.68
- `walkSpeed` 95→120、`chargeSpeed` 880→950、`idleTime` 1.05→0.8、`stunTime` 2.6→2.2
- 體力：`staminaRegen` 34→**25**、`staminaRegenDelay` 0.45→**0.58**、翻滾 22→**25**、滿力斬 62→**64**
  - 算式：滿力斬(64) + 一次翻滾(25) = 89，幾乎榨乾 100 點；回滿要近 4 秒
  - 這正是要的「吃不飽餓不死」——永遠差一點，永遠要取捨

**6. 憤怒系統與發狂視覺（需求 6）**
- 憤怒等級 = 已破壞部位數（0..4），每級：傷害 +20%、速度 +13%、預警 −10%、出招間隔 −14%、撞牆硬直 −9%
- 各項縮短都有上限（預警最多 −42%），避免預警短到看不見而變成不講理
- 視覺發狂（強度全部隨憤怒等級遞增）：
  - 本體顏色朝暗紅推移（`mixRage()`）
  - 身上浮現**發光的熔岩裂縫**，用 `lighter` 合成，條數隨等級增加（2 級加一組、3 級再加、4 級再加一道貫穿）
  - 持續冒出的**火星粒子**，破壞部位瞬間爆發 34 顆
  - 身體**持續微幅顫動**，蓄力狂暴連撞時劇烈抖動
  - 腳下紅色地光 ＋ 外圈紅色脈動光暈
  - 核心從金黃轉為赤紅，脈動頻率加快
  - **畫面邊緣持續泛紅**——玩家不用看 UI 就感覺得到壓力

**7. UI**
- Boss 血條下方新增**部位破壞條**（4 格），接近破壞時轉紅閃爍，破壞後顯示「XX 破壞」
- Boss 名稱旁 4 顆菱形顯示憤怒等級，血條下方顯示「憤怒 N　傷害 +X%　速度 +Y%」
- 部位破壞時畫面中央大字橫幅「XX 破壞 / 守衛更加憤怒了」＋ 0.15 秒頓幀 ＋ 強震動
- 開始畫面加上機制警語：「斬擊可累積破壞守衛的部位 —— 但每破壞一處，牠只會更加狂暴」

### 改動檔案

```
新增：src/boss/parts.ts, src/boss/projectiles.ts
重寫：src/tuning.ts, src/boss/guardian.ts, src/main.ts
修改：src/player.ts（斬擊無敵區間）, src/ui/hud.ts（部位條 + 憤怒）, src/ui/screens.ts（說明更新）
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npx vite build` 通過（52.42 kB，gzip 16.32 kB）
- **仍未實際試玩**，平衡數值全是理論推算

### 下一步建議

1. **先試玩測平衡**，最可能爆掉的三個點：
   - 憤怒 4 級（傷害 +80%、速度 +52%）是否還打得贏 → 調 `rage.damagePerLevel`
   - 「狂暴連撞」撞牆不失衡，在縮小後的場地可能連續逼死玩家
   - 體力收緊後是否綁手綁腳到不好玩 → 調 `staminaRegen`
2. Boss 820 HP 需約 7 次核心命中，配合更短的輸出窗口，可能太長 → 考慮降到 700
3. 加音效——現在完全無聲，7 種招式全靠視覺辨識，聲音會大幅降低辨識負擔

---

## 2026-07-28 — #001 專案建立，第一個可玩版本

### 做了什麼

從零建立專案，完成一個**完整可玩的垂直切片**：可以從開始畫面進入戰鬥，打贏或打輸 Boss 1，並重新挑戰。

**基礎建設**
- Vite + TypeScript（strict）專案骨架，無任何第三方執行期依賴
- `index.html` 全螢幕 Canvas，隱藏系統游標（改由遊戲自繪準心）
- 支援 devicePixelRatio（上限 2x），視窗縮放即時響應

**核心系統**
- `core/vec.ts` — 向量工具。含 `distPointSeg()`（點到線段距離），是斬擊命中判定的基礎
- `core/input.ts` — 輸入層。持續狀態（WASD、左鍵按住）用查詢式；一次性事件（右鍵按下、左鍵放開）用「邊緣觸發 + consume」，確保一個事件只被邏輯處理一次。已擋掉右鍵選單、視窗失焦時清空按鍵
- `core/camera.ts` — 跟隨鏡頭，帶游標方向前瞻與螢幕震動，並夾在競技場邊界內
- `tuning.ts` — **所有數值集中管理**

**玩法（依使用者規格實作）**
- WASD 移動（帶加速/煞車趨近，瞄準時速度降為 55%）
- 左鍵長按 → 時間縮放降到 0.16 的子彈時間，放開沿游標方向位移斬擊
- 右鍵 → 朝游標方向翻滾固定 145px、固定 22 體力，8%～72% 區間有無敵幀
- 血量 100 不自然回復；體力 100，停止消耗 0.45 秒後以 34/秒回復

**斬擊機制（本作核心）**
- `slash.ts` 的 `planSlash()` 是唯一計算來源：距離 60～300px 線性映射到傷害 14～60、耗力 12～62
- 體力不足時**自動裁短**而非禁止出手（`capped = true`），HUD 用紅色虛線畫出「原本想斬到哪」
- HUD 在體力條上直接畫出這一斬要吃掉的區段，並顯示 `−耗力 → 傷害` 文字

**Boss 1「石之守衛」**
- 狀態機：idle → 衝撞預警 → 衝撞 →（撞牆）硬直 / 砸地預警 → 砸地 → 恢復
- 撞牆後硬直 2.6 秒，胸口核心暴露 = 唯一有效輸出窗口
- 打身體 0.15 倍、打核心 2.0 倍，逼玩家去賭那個窗口
- 砸地產生範圍傷害 + 擴散衝擊波（只有環上會傷人）
- 血量越低裂紋越多

**畫面與回饋**
- 開始畫面：塵埃粒子、掃過標題的劍痕動畫、操作說明表、脈動的開始提示
- 死亡 / 勝利覆蓋層（勝利顯示耗時），R 重來、Esc 回標題
- 命中頓幀（核心 85ms / 身體 35ms）、鏡頭震動、傷害跳字、子彈時間暗角、低血量紅色警示

### 為什麼這樣設計

- **`planSlash()` 單一來源**：如果 HUD 預覽和實際執行各算各的，數值一旦不同步，玩家會覺得遊戲在騙人。這是這類「預先讀取風險再出手」的遊戲最不能出的錯
- **體力不足時裁短而非禁止**：直接禁止會讓玩家在緊要關頭「按了沒反應」，體感很差；裁短則保留了操作回饋，同時仍然懲罰了體力管理失當
- **命中用線段一次性判定**：斬擊位移在 0.13 秒內跑最多 300px，逐幀圓形檢測必定穿透
- **打身體幾乎無傷**：如果隨便砍都有效，「誘導撞牆 → 抓硬直」的核心循環就不成立了

### 改動檔案

新增全部 13 個檔案：
```
package.json, tsconfig.json, index.html, CLAUDE.md, DEVLOG.md
src/main.ts, src/tuning.ts, src/slash.ts, src/player.ts, src/arena.ts
src/core/vec.ts, src/core/input.ts, src/core/camera.ts
src/boss/guardian.ts
src/ui/hud.ts, src/ui/screens.ts
```

### 驗證狀況

- `npx tsc --noEmit` 通過
- `npx vite build` 通過（30.55 kB，gzip 10.35 kB）
- **尚未實際試玩**，手感與數值平衡都還沒驗證過

### 下一步建議

1. **先試玩，調手感**——特別是這三個數字：斬擊 `duration`(0.13)、子彈時間 `bulletTimeScale`(0.16)、翻滾 `distance`(145)
2. 平衡驗證：Boss 620 HP 需要約 6 次核心命中，實測可能太長
3. 加音效（斬擊、命中核心、撞牆、受傷）——目前完全無聲，打擊感少一半
4. Boss 第三招（目前只有衝撞和砸地，久了會膩）
