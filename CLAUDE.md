# TITAN SLASH — 專案交接文件

> 這份檔案是「隨時可以開工」的唯一入口。接手前先讀完這頁，再看 `DEVLOG.md` 最新一則。
> **規則：每次改動程式碼後，都必須更新本檔的「目前狀態」章節，並在 `DEVLOG.md` 追加一則記錄。**

---

## 1. 這是什麼

俯視角的 Titan Souls 類型 Boss Rush 遊戲。只有 Boss 戰，沒有雜兵。
核心張力：**斬擊距離越遠 → 傷害越高 → 體力消耗越大**，而體力同時是翻滾閃避的資源。
玩家必須在「保留體力活下去」與「賭一記重斬」之間做選擇。

- 技術棧：TypeScript + Canvas 2D + Vite（無遊戲引擎、無第三方執行期依賴）
- 專案路徑：`C:\MiniGame\TitanGame`
- 目前有兩個章節，Title 畫面可分別點擊進入：**第一章 石之守衛**、**第二章 傀儡師**

## 2. 怎麼跑起來

```bash
cd C:\MiniGame\TitanGame
npm install         # 第一次才需要
npm run dev         # 開發伺服器 → http://localhost:5173（改程式碼即時重載）
npm run build       # 型別檢查 + 打包到 dist/
npm run build:single # 打包成單一檔案 TitanSlash.html（雙擊即玩）
```

改完程式碼一定要跑一次 `npx tsc --noEmit` 確認型別過。

### 想直接在本機打開（不開 dev server）

**用 `npm run build:single`，然後雙擊 `TitanSlash.html`。**

⚠ 不要試圖雙擊 `dist/index.html`——那是打不開的（只會看到黑畫面）。
原因：`dist/index.html` 引用外部的 `<script type="module" src="./assets/xxx.js">`，
而瀏覽器對 `file://` 協定下的 module script 有 CORS 限制，會直接擋掉。

`build-single.mjs` 的工作就是把 JS 內嵌成 inline module，
沒有任何外部請求 → `file://` 就能正常執行。產物是單一 HTML，可以直接傳給別人。

## 3. 操作規格（使用者定義，不要擅自更動）

| 輸入 | 行為 |
|------|------|
| `W A S D` | 上下左右移動 |
| 滑鼠左鍵（長按） | 進入子彈時間，滑動滑鼠瞄準；**放開**即朝該方向斬擊。距離越遠，傷害越高、體力消耗越多。**位移期間無敵——這是唯一的閃避手段** |
| 蓄力中把游標移回**自己身上** | 放開左鍵即取消：不出刀、不耗體力 |

**#011 移除了翻滾**：右鍵／空白鍵不再綁定任何遊戲功能（右鍵仍擋掉瀏覽器選單、空白鍵仍
`preventDefault` 避免捲頁，這兩點是刻意保留的衛生習慣，不是漏刪）。
連帶移除了 `core/input.ts` 的 `chargeLocked`／`cancelCharge()`／`consumeChargeCancel()`——
那一整套「翻滾打斷蓄力」機制的唯一呼叫來源就是翻滾，拿掉翻滾後全部變成死代碼，一併刪除。

### 蓄力取消

放開左鍵時，若游標落在角色周圍 `cancelRadius`(48px) 內 → `SlashPlan.cancel = true`，不出刀。
`cancelRadius` **必須小於 `minDist`(60)**，否則會和「最小距離斬擊」的判定重疊。

判定用的是**游標到角色的實際距離**，不是 `aimVec` 的長度——
`drag` 瞄準模式下 `aimVec` 是拖曳量，長度不代表游標離角色多遠。
因此 `planSlash()` 的第三個參數 `cursorDist` 必須由呼叫端獨立算好傳入。

視覺上：蓄力時角色身上一直有一圈**低調的虛線圈**（讓玩家知道反悔隨時都在），
游標進入時轉為**紅色實線 + 大 ✕ + 「放開以取消」**，且**不再畫瞄準線**——
畫面上不該同時出現「要斬」和「不斬」兩種訊號。游標本身也會變成紅色 ✕。

角色數值：
- **血量**：不會自然回復
- **體力**：停止消耗 `staminaRegenDelay` 秒後開始持續回復
- 體力現在只被斬擊消耗（沒有翻滾了），比原本「滿力斬64+翻滾25」的經濟寬鬆很多，**尚未依此重新驗證平衡**
- 斬擊的 `iFrameEnd`(0.92)/`minCost`(8)/`bulletTimeBlend`(0.06) 都比拿掉翻滾前更寬鬆——
  因為斬擊現在是唯一的閃避手段，要讓短距離的「防禦性一斬」划算、能快速反應

## 4. 檔案地圖

```
根目錄
├─ vite.config.ts       base: './'，讓產物不綁死在網站根目錄
├─ build-single.mjs     把 dist 壓成單一 HTML（見第 2 節）
└─ TitanSlash.html      build:single 的產物，雙擊即玩

src/
├─ main.ts              進入點：場景管理、章節切換（AnyBoss 聯合型別）、主迴圈、時間縮放、命中結算、繪製分層
├─ tuning.ts            ★ 所有可調數值集中在此。調手感只改這裡
├─ fx.ts                特效系統單例（粒子/環/弧/刀光/閃光），玩家與 Boss 共用
├─ slash.ts             斬擊解算 planSlash()：距離 → 傷害/耗力的唯一計算來源
├─ player.ts            玩家狀態機 idle / dodge / slash / dead
├─ arena.ts             競技場（矩形石室）與邊界收束
├─ boss/
│  ├─ types.ts          Boss 共用型別（HitResult），guardian.ts / puppeteer.ts 都從這裡 import
│  ├─ guardian.ts       Boss 1「石之守衛」：狀態機、7 種招式、憤怒系統、命中判定
│  ├─ parts.ts          可破壞部位系統（破壞值累積、部位繪製）——只有石之守衛用
│  ├─ projectiles.ts    石之守衛的場地危險物：衝擊波、投石、地裂
│  ├─ puppeteer.ts      Boss 2「傀儡師」：狀態機、5 種招式、三階段、命中只算本體
│  └─ puppets.ts        傀儡師的特殊傀儡（可斬、回體力）與三種危險物（放射路徑/脈衝/公轉）
├─ core/
│  ├─ vec.ts            向量與數學工具（含點到線段距離，斬擊判定用）
│  ├─ input.ts          輸入層：持續狀態 + 邊緣觸發事件（consume 模式）
│  └─ camera.ts         跟隨鏡頭、游標前瞻、螢幕震動、縮放（`T.camera.visibleFraction`）
└─ ui/
   ├─ hud.ts            血條、體力條（含耗力預覽）、Boss 血條（AnyBoss，Guardian 專屬區塊用 instanceof 分支）、瞄準線、傷害跳字、準心
   └─ screens.ts        開始畫面 TitleScreen（含章節選擇 hitTestChapter）、死亡/勝利覆蓋層
```

## 5. 關鍵設計約定（改動時務必遵守）

1. **`planSlash()` 是唯一的斬擊計算來源。**
   HUD 的預覽與實際執行都呼叫它，保證「玩家看到的數字 = 放手後真正發生的事」。

2. **體力不足時斬擊會自動裁短，而不是不能斬。**
   `SlashPlan.capped = true`，HUD 會用紅色虛線畫出「你原本想斬到哪」。

3. **⚠ 破壞部位會「激怒」Boss，不是削弱牠。**
   這是本作最反直覺、也最核心的設計，改任何 Boss 邏輯前請先理解（詳見第 6 節）。
   曾經一度做成「破壞部位 = 削弱招式」，已於 #002 翻轉，不要改回去。

4. **命中判定用完整軌跡線段（點到線段距離），一次性結算。**
   斬擊位移很快，逐幀圓形檢測會穿透。判定在 `main.ts` 的 `player.slashHitPending` 區塊。
   優先序：暴露的核心 > 最近的未破壞部位 > 身體。

5. **時間流分兩層**：`rawDt`（鏡頭、UI、子彈時間過渡）與 `dt = rawDt × timeScale`（所有遊戲實體）。
   命中時插入 `hitStopT` 頓幀（`dt = 0`）製造打擊感。

6. **投射物的傷害由 Boss 動態注入**（`spawnBoulder(from, target, damage)`），
   不要在 `projectiles.ts` 裡直接讀 `T.guardian.xxxDamage`——那會繞過憤怒加成。

7. **向量一律回傳新物件**，不就地修改，避免別名 bug。

8. **輸入的一次性事件用 `consumeXxx()`**，確保一個事件只被處理一次。

9. **玩家永遠畫在 Boss 之上**（`main.ts` 的 render 順序），
   且 `separatePlayerFromBoss()` 會把玩家推出 Boss 的碰撞圓。
   兩者並存的理由：推開處理常態，繪製順序處理斬擊穿越時的短暫重疊。
   **斬擊位移期間不推開**——那是穿越攻擊，只有 0.13 秒。

10. **特效一律透過 `fx` 單例**（`src/fx.ts`），不要在各系統自己養粒子陣列。
   `fx.update()` 吃的是**已縮放的 dt**，所以子彈時間裡特效會慢、命中頓幀時會定格——
   這是刻意的，不要改成 rawDt。
   粒子總量上限 900，超過會丟棄最舊的，避免長時間戰鬥掉幀。

11. **`pickAttack()` 打不到人就不出招，不是只調低機率。**（兩隻 Boss 都適用，#009）
    範圍以自己為中心的招式（石之守衛的砸地/橫掃、傀儡師的環繞召喚）離玩家太遠時
    **直接不列入候選**；需要助跑距離的招式（衝撞、狂暴連撞、傀儡師的直線衝刺）太貼近時也排除。
    `pickAttack()` 回傳型別是 `... | null`，選不到候選就回傳 `null`，
    `updateIdle()` 這時不出招、繼續走位，等下一幀距離夠了才會出招。
    新增招式時記得同步判斷它是否是「以自己為中心」或「需要助跑」，決定要不要加距離排除。

12. **兩隻 Boss 共用 `AnyBoss = Guardian | Puppeteer` 聯合型別**（`main.ts`/`hud.ts` 各自定義，
    沒有另外包一層抽象介面——目前只有兩隻 Boss，直接聯合型別＋`instanceof` 分支比硬做通用介面務實）。
    `tryHit(a, b, baseDamage, playerPos)` 的簽章兩者必須一致，即使某一方用不到 `playerPos`
    （石之守衛目前忽略它），這樣 main.ts 才能用同一行呼叫任何一種 Boss。
    `HitResult` 型別集中在 `boss/types.ts`，避免兩個 boss 檔案互相 import。
    HUD 的 `rage`/`rageRatio` 欄位兩者共用（石之守衛＝憤怒等級，傀儡師＝目前階段），
    但部位破壞條這類真正 Guardian 專屬的畫面用 `instanceof Guardian` 分支處理，不要硬塞進共用介面。

13. **鏡頭縮放後（#011），玩家看不到全地圖，Boss 一定要搭配場外提示。**
    `Camera.zoom` 依 `T.camera.visibleFraction` 算出；`Camera.isOnScreen()` 判斷世界座標是否在畫面內，
    `hud.drawOffscreenBossIndicator()` 在 Boss 不可視時於螢幕邊緣畫箭頭（顏色依兩隻 Boss 共用的
    `isThreatening` getter 切換冷色/警示色）。**這個呼叫必須放在 `hud.draw()`（畫 Boss 血條）之後**，
    不然 Boss 在正上方時箭頭會被血條的不透明底色蓋住——這是實測抓到的 bug，已修正並在
    `main.ts` 留了註解。上方的邊界比其他三側大（`topPad` vs `pad`），避開血條/部位條那塊 UI。
    這次範圍只做 Boss 本體的提示，不含個別傀儡/投石/預警圈子物件。

14. **任何決定「攻擊會出現在哪」的隨機數／方向，都要在選招當下鎖定，不能等出招瞬間才決定**
    （#013、#016）。這不是只有 `facing` 要注意——招式a 的路徑角度（`radialBaseAngle`）、
    招式c 的走廊角度（`computeGauntletCorridors()`）都曾經犯過同一種錯：預警畫面用一組
    亂數（甚至是純裝飾動畫），出招瞬間又重算一組新的，兩者對不上，等於整段預警是假情報。
    **新增任何會畫預警的招式時，檢查兩件事**：(1) 決定最終攻擊位置/角度/方向的隨機數，
    是不是在 telegraph 一開始（或選招當下）就算好存進欄位，而不是每幀重算或留到
    telegraph 結束才算；(2) 畫預警的函式讀的是不是**同一個**存好的值，不是另外重算或
    裝飾用的假動畫。兩隻 Boss 都有 `snapFacing(target)`（瞬間鎖定朝向，不像 `faceToward`
    是漸進的），在 `updateIdle()` 選好招式、`enter()` 之前呼叫；預警階段的 update 函式裡
    絕對不要呼叫 `faceToward`。

15. **斬擊路徑上的目標全部算數，不是只吃第一個。**（#013）`main.ts` 的命中結算裡，
    特殊傀儡（回體力）與 Boss 本體（傷害）用同一條軌跡線段各自獨立判定，不互斥；
    `Puppets.trySlashHeal()` 也會掃過全部特殊傀儡而不是命中就提早回傳。
    新增「可被斬到」的物件時，記得比照這個「不互斥、掃全部」的規則，不要不小心做成互斥。

## 6. Boss 1「石之守衛」

### 6.1 憤怒系統（核心）

**每破壞一個部位，憤怒 +1 級（0..4）**，Boss 全面強化：

| 每級效果 | 數值 |
|---------|------|
| 攻擊傷害 | +20% |
| 移動 / 衝撞速度 | +13% |
| 預警時間縮短 | −10%（上限 −42%，保證預警看得見） |
| 出招間隔縮短 | −14%（上限 −52%） |
| 撞牆硬直縮短 | −9%（上限 −34%，輸出窗口越來越短） |

玩家破壞部位換到的是：
- **破壞處變成永久弱點（1.6 倍傷害）**——這是最主要的回報，也是整個系統的平衡對重
- 破壞瞬間的額外傷害（部位 integrity × 0.18）與**踉蹌硬直**（一個輸出窗口）
- **核心護甲破壞後，核心永久暴露**——不必再誘導撞牆，這是回報最高的目標

沒有「破壞處變弱點」的話，拆部位就只有壞處，玩家沒有理由去拆——
整個部位系統會退化成一個純懲罰機制。這條規則是它成立的前提。

所以「拆哪個、拆幾個、什麼時候拆」是這場戰鬥真正的策略層。

### 6.2 可破壞部位

| 部位 | 位置（相對面向） | 破壞值 | 未破壞時倍率 | 破壞後 |
|------|-----------------|-------|------------|-------|
| 核心護甲 | 正前方 | 300 | 0.3 | 核心永久暴露（2.0 倍） |
| 左臂 | 左前 66° | 155 | 0.5 | 傷口 1.6 倍 |
| 右臂 | 右前 66° | 155 | 0.5 | 傷口 1.6 倍 |
| 腿部 | 正後方 | 185 | 0.45 | 傷口 1.6 倍 |

**傷害倍率總表**（判定優先序由高而低）：

| 目標 | 倍率 | 說明 |
|------|-----|------|
| 暴露的核心 | **2.0** | 撞牆硬直中，或核心護甲已破壞 |
| 裸露傷口 | **1.6** | 已破壞部位的位置，永久弱點 |
| 未破壞裝甲 | 0.3～0.5 | 低傷害，但會累積破壞值 |
| 石殼本體 | 0.12 | 幾乎無效 |

- 傷口的判定**優先於裝甲**：玩家是刻意瞄準傷口，不該因為旁邊的裝甲剛好更近而被吃掉
- 核心與護甲都長在**正前方**，繞到背後只打得到腿——但腿破壞後，繞背就有了價值

### 6.3 招式（依破壞部位數解鎖）

| 憤怒 | 解鎖 | 招式 |
|-----|------|------|
| 0 | 起始 | **衝撞**（撞牆 → 硬直，唯一的天然核心窗口）、**砸地**（範圍 + 擴散衝擊波）、**橫掃**（近距離扇形）、**普攻**（見下） |
| 1 | 破壞 1 處 | **投石**（遠距離牽制，落點有預警圈）、**三連踏**（逐次逼近的三發踏擊） |
| 2 | 破壞 2 處 | **地裂**（放射狀波前，要站在縫隙間） |
| 3 | 破壞 3 處 | **狂暴連撞**（三段高速衝刺，撞牆也不會失衡） |

**衝擊波與地裂只有「波前」會傷人**，站在圈內或衝過去都安全——這是可以練出來的閃避技巧。

**普攻**（#010 新增，`telegraphPunch → punching → telegraphHeavyPunch`）：
原地兩記快速直拳（圓形，正前方）＋自動蓄力補上一記範圍更大更痛的正面重擊（一樣是圓形）。
節奏比其他讀招快、前搖短，且**不受「避免連續同招」限制**，`pickAttack()` 選招時可以連續使用，
呼應「普攻可以相接」；只有打得到人的距離才會被選中，不看憤怒等級。

### 6.4 兩條攻略路線

- **保守**：完全不拆部位，只誘導衝撞撞牆，抓天然的核心窗口。Boss 永遠只有 3 招，但輸出窗口稀少
- **激進**：拆掉核心護甲換取常駐核心，然後在牠最狂暴的狀態下速殺

## 7. Boss 2「傀儡師」（#012 全面重做，取代 #006~#010 的舊設計）

### 7.1 核心玩法：斬擊是唯一的閃避＋前進手段

**只有打到本體才會受傷**（`bodyMult = 1.0`）——沒有部位、沒有核心/護甲倍率分層，
難度來自「打得到」而不是「打得對」。地面危險空間被壓縮，玩家要靠斬擊的位移無敵幀
穿梭到安全點、一路逼近本體，跟移除翻滾（#011）是同一條設計主軸的延伸。

待機時**持續往玩家身上逼近施壓**（#007~#008 沿用不變，見下方待機邏輯）：
只有貼到 `retreatDist` 才會退，超過 `approachDist` 全速追，中間用較慢速度持續貼近。
每連續出招 `opening.every` 次會有一段明顯更長的**破綻**（畫面「破綻」＋淡綠外環），
保證玩家有看得懂的輸出節奏點。

**她本人沒有無敵/硬直窗口機制**（招式c 期間例外，見 7.3）：任何時候本體都可以被打中。
`HitResult.kind` 回報 **`'core'`**（重用既有 kind）——本體命中在敘事上等同「打到核心」，
直接沿用 main.ts 既有的金色特效與大數字回饋。

### 7.2 特殊傀儡（`SpecialPuppet`，`boss/puppets.ts`）

貫穿全部招式的核心機制：

- **不攻擊**，純環境物件
- **玩家靠近會延遲爆炸**：進入 `triggerRadius` 開始累積引信，離開會衰退（比累積快，
  不是瞬間歸零）；引信滿了在 `explodeRadius` 內造成傷害
- **被斬中會回復體力**（`staminaRestore`）並消滅——`main.ts` 的命中結算裡，特殊傀儡跟本體
  用同一條斬擊軌跡線段**各自獨立判定，不互斥**（#013）：一斬只要幾何上都掃到，回體力跟
  傷害本體可以同時發生；`trySlashHeal()` 也會掃過全部特殊傀儡，不是命中第一個就提早回傳
- 外觀跟一般傀儡（骨白）、傀儡師本人（深紫）都不同：暖色青綠 `#7fe0c9`，
  一圈隨接近而填滿的引信進度環
- 支援可選的 `orbit` 參數（中心/半徑/角速度），讓它可以跟著一圈危險物一起公轉（招式d／招式e）
- **有存活時間**（`special.life`＝5秒，#014→#015 從 8 秒調降）：沒被斬到也沒炸到的話，
  時間到會自動淡出消失（消失前 1 秒開始淡出），避免玩家不理它們、場上越堆越多隻。
  這個值刻意比招式c 的 `gauntlet.duration`(6.5s) 短，走廊上的特殊傀儡可能提早消失，
  逼玩家別在走廊裡慢慢摸。招式d／招式e 公轉的那隻沿用 `spawnOrbitRing` 自己的 `life`，
  跟整圈危險物同壽命，不是套通用值

### 7.3 血量三階段與招式

| 階段 | HP 門檻 | 解鎖招式 |
|-----|--------|---------|
| 0 | 100%→66% | 招式a（放射狀路徑）、招式b／b'（前撲，兩種基本招） |
| 1 | 66%→33% | ＋招式e（小型旋轉環，招式d 的低風險預告版），出招間隔加快 |
| 2 | ＜33%（最終階段） | ＋招式c（傳送穿越）、招式d（中心旋轉環） |

**進最終階段時會強制先放一次招式d**（`centerIntroDone` 旗標，沿用 #011 的強制開場機制，
這次改成觸發招式d）：`updateIdle()` 選招前判斷，不透過 `pickAttack()` 加權隨機，
確保玩家一定會看到這個階段的招牌動作。

| 招式 | 觸發時機 | 說明 |
|-----|---------|------|
| a 放射狀路徑 | 全程可用 | 向外打出等角度間隔的路徑（`RayHazard`，telegraph 後瞬間拉長），每兩條路徑的中間空隙都放一隻特殊傀儡。角度在選招當下鎖定（`radialBaseAngle`），預警畫的就是最終路徑，不是等出招瞬間才重算（#016） |
| b 前撲＋後跳 | **全程可用，基本招** | 前搖短、單次扇形判定（`forwardStrike()`）、後跳拉開距離，**在她出招的原地留一隻特殊傀儡**——自然教會玩家「特殊傀儡可以斬」，不受「避免連續同招」限制，可連續使用 |
| b' 前撲，不後跳 | **全程可用，另一種基本招**（#015 新增） | 跟招式b 共用同一個 `forwardStrike()` 判定，數值另外設在 `T.puppeteer.press`，差別是打完直接收招、留在原地不撤、不留特殊傀儡——跟招式b 互補（一個換獎勵、一個換連續輸出），同樣不受「避免連續同招」限制 |
| e 小型旋轉環 | 階段1解鎖（#015 新增） | 原地召喚一圈公轉危險物（`T.puppeteer.spin`：半徑140/5隻/3秒，比招式d 小、少、短），其中一格是特殊傀儡——完全重用 `spawnOrbitRing()`，是招式d 的低風險預告版，讓玩家先在這裡練習「抓時機貼近旋轉中的特殊傀儡」 |
| c 傳送穿越＋暈眩 | 階段2解鎖 | 傳送到「玩家位置以競技場中心鏡射」的位置，短暫 `fieldDelay` 後鋪滿全場脈衝傀儡（`PulsePuppet`），只留 `corridorCount` 條安全走廊（保證一條是本體→玩家直線），走廊上擺特殊傀儡。**這招啟動期間打中本體 → 暈眩她 `stunDuration` 秒**並立刻清空危險物，是保底輸出窗口；`duration` 秒安全逾時未命中則自動收招 |
| d 中心推牆＋旋轉環 | 階段2解鎖，**最終階段強制開場** | 跳到場地中心，一發擊退把玩家推開（沿用 `player.takeDamage` 既有擊退），接著召喚一圈公轉危險物（`OrbitHazard`），其中一格換成會公轉的特殊傀儡——玩家要抓時機貼近它藉機欺身 |

暈眩（招式c）的觸發邏輯在 `tryHit()` 命中後呼叫 `maybeTriggerStun()`：
只在 `gauntletActive` 且危險場地已經灑出（`gauntletFieldSpawned`）時才會觸發，
避免傳送剛完成、場地還沒鋪好就被誤觸發。

**沒有保留舊版的「連續命中拉扯反擊」**：舊機制是「打太猛就被推開」，
跟新設計「玩家要拚命靠近本體」的核心張力直接衝突，這次重做時判斷移除，不是遺漏。

### 7.4 終局處決（全場僅觸發一次，#017）

最終階段血量跌到**階段內剩一半**（`triggerHpRatio = phase3Hp/2 ≈ 0.165`，不是總血量的
一半——那樣會在進最終階段前就先觸發）→ `applyDamage()` 標記 `executeTriggered`，
**血量鎖住不再下降**，並排定 `executePending`，下一幀 `update()` 開頭就**強制打斷她當下
正在做的任何事**，進終局序列：

1. 跳到場地正中央，玩家沿「本體→玩家」方向**直接位移**（不是物理擊退）夾在牆邊
2. `buildExecutePath()` 從玩家位置到本體位置，固定步距（略小於斬擊 `maxDist`）逐步前進
   加隨機橫向偏移，生出一串安全圓——**連通性靠生成方式保證**，不是搜尋算出來的，
   不會有卡死玩家的情況
3. 全場網格灑滿脈衝傀儡（`PulsePuppet`），排除掉落在任何安全圓內的格子；安全圓
   （除了玩家起點與本體所在的最後一點）都放一隻特殊傀儡——這招**沒有時間上限**，
   所以危險物跟特殊傀儡的存活時間都設近乎無限（`execute.infiniteLife`）
4. `tryHit()` 在這個狀態下命中本體，**不扣血、直接觸發處決**：清場、進 `executing`
   定格，`HitResult` 多回報 `executed: true`；main.ts 收到後觸發放大/慢動作演出
   （`Camera.execZoomMult` 依正弦曲線放大再收回，`timeScale` 強制壓到比一般子彈時間更慢），
   `executing` 倒數 `deathDelay` 後才真的變 `dead`，讓既有的勝利流程自然接手

### 7.5 外觀

跟石之守衛一樣全部用幾何路徑畫，刻意做出「有差別但不多」的區分：
傀儡師是偏瘦長的菱形/兜帽輪廓，深紫色（`#5b3a68`）；一般傀儡是矮胖的橢圓，
骨白/舊木色（`#d8cfc0`），身上有十字縫線裂痕暗示木偶關節；特殊傀儡是青綠色（`#7fe0c9`），
額外一圈引信進度環，三者顏色都不同，一眼可辨。

## 8. 目前狀態（最後更新：2026-08-04，DEVLOG #017）

**已完成且可玩**：
- 完整操作三件套（移動 / 子彈時間斬擊 / 翻滾），斬擊與翻滾皆有位移無敵幀
- 血量與體力系統，含體力回復延遲、耗力預覽 UI
- Boss 1 石之守衛：7 種招式、4 個可破壞部位、憤怒系統、發狂視覺
- 開始畫面（含操作說明與機制警語）、死亡畫面、勝利畫面（含計時）
- 鏡頭跟隨與震動、子彈時間暗角、憤怒紅色邊緣、低血量警示、傷害跳字、頓幀、部位破壞橫幅
- **完整特效系統**：玩家斬擊刀光/翻滾塵土/受擊火花，Boss 全七招各自的專屬特效，
  命中依部位分級（核心金色 / 傷口橘紅 / 裝甲石屑 / 本體冷色彈開），勝利崩解爆發
- **單一檔案打包**（`npm run build:single` → `TitanSlash.html`，雙擊即玩）
- 蓄力取消區（游標拉回身上即放棄）、旋轉瞄準環游標、玩家與 Boss 不重疊
- **Boss 2 傀儡師（#012 全面重做）**：命中只算本體（無部位分層）、4 種新招式（放射狀路徑/
  前撲基本招/傳送穿越+暈眩/中心旋轉環）、核心新機制**特殊傀儡**（不攻擊、靠近延遲爆炸、
  被斬回體力）、三階段（進最終階段強制先放招式d）
- Title 畫面新增**章節選擇**（點擊「第一章．石之守衛」/「第二章．傀儡師」個別進入，
  按任意鍵仍固定進第一章）；勝負文案依章節切換
- 石之守衛新增**普攻**連段（原地雙拳＋蓄力重擊，可連續使用，只在打得到人的距離才會被選中）
- **移除翻滾**（#011）：斬擊位移無敵幀是唯一閃避手段，`iFrameEnd`/`minCost`/`bulletTimeBlend` 因此調寬
- **鏡頭縮放**（#011）：`T.camera.visibleFraction`(0.575) 讓玩家看不到全地圖，
  搭配場外提示（`hud.drawOffscreenBossIndicator`，須畫在 `hud.draw()` 之後避免被血條擋住）
  避免 Boss 從看不到的地方偷襲
- **方向鎖定於選招當下**（#013/#016）：兩隻 Boss 所有會決定攻擊落點/朝向的隨機值
  （`snapFacing`、放射狀 `radialBaseAngle`、走廊 `gauntletCorridors`）都在選招瞬間鎖定，
  預告畫面用同一份鎖定值，絕不在提示消失後才轉向玩家
- **斬擊多目標判定**（#013）：一斬同時檢查特殊傀儡回復與本體傷害，兩者不互斥
- **特殊傀儡壽命**（#014/#015）：`T.puppeteer.special.life`(5s) 到期淡出消失，避免全場堆滿
- **傀儡師新增招式**（#015）：普攻 b'（前揮不後跳）、第一階段限定小型旋轉環（招式e）
- **傀儡師終局處決**（#017，全場僅觸發一次）：進最終階段且血量跌破階段內一半
  （`T.puppeteer.execute.triggerHpRatio` 0.165）時鎖血，跳到場中心把玩家推到邊緣，
  全場鋪滿危險物只留下用斬擊距離串起、保證連通的安全圓路徑（`buildExecutePath`），
  安全圓內特殊傀儡壽命近乎無限；斬中本體觸發子彈時間放大鏡頭演出（`camera.execZoomMult`）
  後直接死亡

**尚未做**：
- 音效與音樂（目前完全無聲）
- 美術資源（全部是幾何圖形，無 sprite / 動畫）
- 第三隻以後的 Boss、正式 Boss 選單（存檔）——目前只是 Title 畫面兩個入口，不是真正的選單系統
- 手把支援、按鍵重新綁定
- 個別危險物（脈衝/公轉/路徑）的場外提示——目前只做 Boss 本體

**已知待驗證 / 風險**：
- **傀儡師整體（#012-#016）全套設計完全沒有實測過**：`T.puppeteer` 整區塊（`special`/`radial`/
  `lunge`/`press`/`spin`/`gauntlet`/`center`）的傷害/範圍/時機都是第一版猜測。特別要注意：
  招式c 走廊寬度/場地密度 (`gridSpacing`) 會不會太窄/太密導致走不過去；
  特殊傀儡的引信時間 (`fuseTime`) 跟斬擊的距離/瞄準節奏搭不搭得起來；
  招式d 公轉危險物速度玩家追不追得上特殊傀儡的節奏，都需要實際玩一場才知道
- **傀儡師終局處決機制（#017）完全沒有實測過，是目前最高風險項目**：需要在最終階段把她打到
  `T.puppeteer.execute.triggerHpRatio`(0.165) 以下才會觸發，一般試玩前段不會遇到。
  最需要驗證的是**安全路徑本身**：`buildExecutePath` 每一步是否真的落在斬擊可達距離內、
  `jitter` 隨機橫向偏移會不會把路徑點推進場地邊界或卡住；其次是處決演出的頓幀/
  `execZoomMult` 放大節奏、`cinematicTimeScale`(0.06) 慢動作的觀感是否過長或太短
- **翻滾移除後的手感完全沒有實測過**：斬擊當唯一閃避手段容錯率會不會太低、體力經濟比原本
  「滿力斬+翻滾」寬鬆很多是否需要重新收緊，都需要實際玩一場才知道
- **鏡頭縮放與場外提示完全沒有實測過**：`visibleFraction`(0.575) 這個縮放程度好不好抓、
  箭頭指示看不看得懂，都需要實際玩一場才知道
- **石之守衛的整體平衡完全沒有實測過**。理論值：Boss 820 HP，核心滿斬 120 傷害 → 約 7 次核心命中
- 憤怒 4 級時傷害 +80%、速度 +52%，**可能過於嚴苛**。若試玩後覺得不可能贏，優先調 `T.guardian.rage.damagePerLevel`
- 場地已縮小到 1100×820，Boss 衝撞很快就會撞牆——若覺得核心窗口太好拿，可再放大場地或縮短 `stunTime`
- 「狂暴連撞」撞牆不失衡，在小場地可能連續逼死玩家，需實測
- 本次修改把 `tryHit()` 簽章加了 `playerPos` 參數（兩隻 Boss 都要），
  如果之後再加第三隻 Boss，記得同步這個簽章，否則 `AnyBoss` 聯合型別呼叫會炸

## 9. 接手時的第一步

1. `npm run dev` 開起來，兩個章節都實際玩一場，確認手感（尤其傀儡師，完全沒人試玩過）
2. 讀 `DEVLOG.md` 最新一則，看上次做到哪、下一步原本打算做什麼
3. 要調手感 → `src/tuning.ts`；要改機制 → 對應的系統檔
4. **改完務必更新本檔第 8 節 + 追加 `DEVLOG.md`**
